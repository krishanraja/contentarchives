---
repo: krishanraja/contentarchives
product: contentarchives
as_of: 2026-09-24
head: 58cf1ac
lifecycle: building
production_url: none
state_doc: RESUME.md
history_log: docs/history/LOG.md
truth_files: []
authority_order: [state/STATE.json (generated by tools/track.py and counted from disk), state/PROGRESS.md (generated and the same in prose), state/origin-folders.csv (generated by tools/origin_map.py), RESUME.md, docs/HANDOVER.md, docs/LEARNINGS.md, docs/ARCHITECTURE.md, docs/ORGANISING.md, README.md]
steward: https://github.com/krishanraja/control-center/blob/main/docs/steward/RUNBOOK.md
never_publish: [family names and personal folder names, identity or medical or financial documents (the flagged files and the documents named in the rescue record), local paths carrying a user name, drive labels or models or serials (including the one in the rescue record's filename), machine names (including the from-<name> source-set labels in docs/MIRROR.md and RESUME.md), any person's name or profile name appearing in the docs (ORGANISING.md folder examples and the rescue record) and the PERSON-x pseudonyms, cloud account ids and Drive folder paths, descriptions of the intimate or sensitive content itself]
---
# contentarchives: where it is right now

## What it is

contentarchives is a Python toolkit for consolidating scattered personal media (phones, old laptops, external drives, cloud exports, digitised tapes) into one deduplicated, date-organised library without destroying anything on the way, and it is also the canon of the in-flight consolidation it was extracted from: several sessions on more than one machine read and write this repo as the shared record of where that work stands. Its one rule is the README's: "A path is never sufficient grounds to delete." Deletion is allowlist-only and camera-original filenames are protected unconditionally; one deliberate exception exists, content Krish personally named for permanent destruction, built with its own journal-first, re-hash-at-delete guards rather than a shortcut around the existing allowlist. The conveyor spans thirteen stages plus `guards/`: 151 of 151 code files owned, 77 learnings, 69 enforced by a named function or test, 8 still enforced only in prose (8, 13, 17, 24, 29, 65, 68, 69) (`tests/test_stage_contracts.py`, run 2026-09-24). The consolidation of scattered sources is finished; D: and Google Drive both hold 85,480 files / 926.5 GB, path for path, with zero duplicate content. The newest work answers Krish's own objective, a tool he can "talk" the library to: 85,368 descriptions are embedded for semantic search (`stages/08_index/embed_descriptions.py --ask`) and the library is grouped into 1,388 time-clustered events (`stages/08_index/build_events.py --ask`), each answer stating plainly what it could not see.

## Who it is for and why it matters for Mindmake

Nothing here is sold. It is a personal-operations engine built by one person with AI, whose safety rules were paid for with permanently lost files and written down with the incident behind each one (`docs/LEARNINGS.md`: "Every rule here was paid for"). For the room_face buyer in `control-center/docs/ICP.md`, a senior leader at a PE or VC backed media, adtech, publishing or data business, this repo proves two things: that one person can run an unforgiving data job on AI agents across machines without losing control of it, and that the discipline for doing so is written down in public with the failures left in.

Stories a writer can carry without asking Krish:

- **Asking the system whether it actually learned produced an honest, unflattering answer, and the fix was to write it down.** After a day that shipped six new tools, three new test files and closed sixteen real defects, `docs/LEARNINGS.md` still read 69 rules, unchanged since morning: the lessons had been written into STAGE.md rows and test docstrings, where only a reader of that one stage would ever find them. Eight new rules were added, each a class of failure rather than a restatement, from a structural change reaching every writer that still builds the old layout to a count not being evidence until you know what it counted. The contract test moved from 61 enforced of 69 to 69 enforced of 77 in the same pass, and the prose-only debt stayed exactly the eight rules it already was (`58cf1ac`).
- **A safety check can pass for weeks by consulting a record instead of the thing the record is meant to describe.** The identity-document check read a list of files already flagged and asked whether those files were still present; with one row on the list it always passed, while two passports, a visa stamp, three medicare cards and two licences sat unflagged in the chronology. The file's own comment had already named this exact failure, about a different check, days earlier. Fixed to sweep the chronology by filename instead of trusting the list, proved able to fail before being trusted, then found and moved eleven documents (`f67166d`).
- **The one truly irreversible action gets more guardrails, not fewer.** When Krish said "purge all intimate content forever," the toolkit could not reuse its own allowlist-deletion path, which refuses unless a surviving copy is proven - and there is deliberately no surviving copy of this. It got its own pair of guards instead: every victim named individually with no globs or path substrings (the same class of rule that destroyed 45 files here once already), a re-hash of each file at the instant of deletion, and a journal written first. Even so, a first pass left traces behind - 7 thumbnails, 5 face vectors, 117 tag rows - because it swept by the files it could still delete rather than by the content it was told to destroy; a second pass closed it, verified from the filesystem, never from the tool's own report (`71210d1`).
- **A guard that fires on a schedule can miss the event that matters.** The first real trial of the cloud-mirror uploader picked the six largest files in the library to test with, including one 101 GB file of pure null bytes already identified as a failed copy - and the throttle meant to stop a runaway upload checked every 25th file, so those six never tripped it. 81 GB of zeros reached the cloud before it was stopped by hand. Fixed to check before every file over 2 GB, a byte budget rather than a file count (`65a9103`).
- **A guard can be watching and still unable to say no.** The mirror's stop-on-failure gate logged its own findings through a helper that piped the same text into its return value, so `[bool]` of a non-empty array read as true no matter what the gate actually decided - a step built specifically to stop the run on a partial upload said "failing on purpose so the task restarts," returned false, and was read as success, leaving 172.5 GB unsent while the task sat idle overnight. Fixed so the run's answer is the gate's *last* emission, not whatever it happened to print along the way (`c4df90e`, learning 58).
- **A tool can break silently and stay broken for days.** `tools/refresh.py`, the first command this repo tells a resuming session to run, had been failing at exit 2 since a stage migration renamed the script it called - two days before anyone noticed, during which `state/PROGRESS.md` told every reader the library held 73,821 files while the index already held 82,112. Fixed to take repo-relative paths and refuse a missing step loudly rather than silently omitting it (`1d2bb9c`).
- **A convincing number is not a true one, and it keeps recurring.** One session corrected the same measurement three times in a row: 844 named clusters were first reported with an unanswered sibling covering 22,763 photographs, corrected to 463 siblings / 6,180 photographs once double-counted pairs were deduplicated, then corrected again to a true incremental gain of 39 photographs once it was shown that a sibling's reach is almost always already covered by its named partner (`2e12d5b`, `5ebc787`, `bf10c35`).
- **A measured table, not a guess, settles where to draw a line.** Asked to tighten a floor on a naming game, the session swept six candidate settings and showed Krish the one number that actually mattered: the share of people he had already successfully named that a tighter floor would stop showing him. He chose 0.12 with that table in front of him, not a felt number (`cf0ddaf`).
- **A green report is not proof, and it keeps recurring.** Rule 23: a copy job reported "0 failures, and it was telling the truth" while later writes destroyed earlier ones. The same shape returned on 2026-09-16, when `check_repeats.py`'s baseline read zero earlier sheets and printed "CLEAN, every row is new" for a page about to be sent to Krish (`75bb274`).
- **A journal whose columns the consumer cannot read is worse than no journal.** A tool moving 66,047 files wrote its journal with headers `When, From, To, Prefixed`; the tool meant to repoint every record from that journal reads `source` and `destination`. Run as written, it would have loaded zero moves, verified its own zero changes against the filesystem, and promoted an inventory in which all 66,047 paths were still stale - every line of that output true, the result a lie. The same shape recurred twice more the same week, on `identity-docs-moved.csv` and on the answers journal (`c25e67a`, learning 67; `f67166d`; `ceaaf70`, learning 74).
- **A row with no hash is not a row with a gap, it is invisible.** The documented three-step route for new material - ingest, reconcile, build the index - had never computed a content hash anywhere in it. After 11,707 files went through it clean, the classifier, the face pass and the naming game reported only 116 files left to do, because a hash-less row cannot reach any of them and nothing along the way said so. This hole opens on every ingest, sitting under the VHS captures, the ten communal phones and the photo albums still queued (`00beed1`).
- **An absolute rule can still have a door nobody built a lock for.** Stage 06's invariant reads "existing cluster ids are FROZEN" - and a tool existed to honour it for faces arriving from video frames, but nothing existed for faces arriving from new photographs, which is what every ingest produces. "Never renumber the old ids" and "never hand out an id somebody else is using" are different rules, and only the first had been written down, so 6,970 new clusters landed on top of 15,326 the video assigner already held, six carrying one of Krish's own answers. Repaired from backup and re-run onto a floor that is now a maximum across every allocator (`3755998`, `37a3dc9`).

Objection it answers: "AI agents will quietly wreck our data." Here is the record of one doing exactly that, once, and the rules that made it unrepeatable, plus the newer record of one auditing its own honesty about how much it has actually learned, and finding the answer wanting.

## Where it is right now (as of 2026-09-24)

Lifecycle: building.

- **The library answers in words and in events, which is the objective Krish set for this stage of the work.** 85,368 descriptions are embedded (`gemini-embedding-001`, 768 dimensions, $0.90 measured, 24 minutes, 0 failed) and answer questions whose words appear nowhere in the data; 1,388 events, each a run of photographs with no gap longer than 14 hours, group 46,625 photographs into what a person would call one memory rather than forty scattered hits. Every answer states what it could not see: 99.9% of descriptions searched, 37,296 clockless files in no event at all (`40cd00c`, `a9d4da7`).
- **Two axes an answer still cannot see are named, and one of them is a hard ceiling.** `place` is 52% and cannot be improved by rerunning anything - only 40% of the library carries GPS at all, so the rest is a model's guess from the picture. `date_taken` is 56%, capped by EXIF stripped in transit for stills with nothing left to read. Chasing either needs a better model or a recovered clock, not a rerun.
- **The cloud mirror was re-proven without a token, and the source archives it verified are gone.** `verify_drive_by_mount.py` read 444 sampled files back through the H: mount and matched all 444 against their recorded md5, using the mount's own standing authentication rather than the Drive API reauth that has stalled since 2026-09-18. The four Communal-2026-09-22 source zips and their 24,286-file extraction tree are deleted, 162 GB freed, D: free space up from 3,531 to 3,722 GB; the library itself is unchanged at 85,480 files / 926.5 GB (`d38e973`).
- **Eleven identity documents were found and moved, after the check meant to catch them was itself fixed.** It had asked whether a one-row list of already-flagged files was still present rather than asking the library; it now sweeps the chronology by filename and correctly told apart four real documents from a joke filename in a proof run before being trusted. Krish's own document and a family member's passport and driving licence, both arrivals in the 2026-09-22 batch, are now filed in `Archive\`, journalled and reversible (`f67166d`).
- **The naming rounds on the new material are finished, and a correction bug that would have silently failed was caught by Krish changing his mind.** Three gated rounds added 1,117 answers and 12 people new to the library. `build_game.py` had been reading the answers journal as a set rather than a chronological record, so routing an already-declined row to Bharti's game would have looked applied and done nothing; fixed so the last answer for a group decides. Her queue now stands at 1,192 clusters (`8b31235`, `ceaaf70`, learning 74).
- **Every face the Communal ingest produced is now correctly clustered, after the tool built to catch them broke the id freeze it existed to protect.** Repaired from backup and re-run onto a floor that is a maximum across every allocator; 57,565 video faces and 26,646 photograph faces are placed with no more one-shot or hole-in-the-invariant defects between here and the VHS, phone and album ingests still queued (`3755998`, `37a3dc9`, `234065b`, `ba0d821`).
- **The four Communal Takeout archives are in the library.** 163 GB, Communal 12,134 -> 23,841, every one of 13,266 media members accounted for, 0 failed, 0 undated. Running it surfaced six defects in the documented ingest chain, all closed the same day (`a9833bb`, `00beed1`, and five more; see "What changed recently").
- **The chronology is one level deep and split Personal/Communal, device by device**, both copies matching file for file; detail is in `docs/history/LOG.md` rather than repeated here.
- **Nothing still to run needs the Anthropic API**, which Krish has turned off; classification, faces, embeddings and the video pipeline are all Gemini or local.
- **The repository stays public**, Krish's decision on 2026-09-15: commit messages and docs may name the operators next to photograph counts, but the answers journal, face data and anything the publication tripwire catches stay off GitHub.
- **Standing approval to push to `main` by default**, given by Krish on 2026-09-11.
- **Largest open risk, updated**: `gcloud auth login --enable-gdrive-access` still blocks emptying Drive's trash and running the stronger, server-side `verify_drive_md5.py --refresh`; the mount-based proof above is interim evidence, not a replacement for it, and neither Google account's token can be refreshed from a script. Also open: a judgement call on 294 files / 1.29 GB of finished video-project renders and a podcast recording that are personal but not camera originals. Otherwise the front line is new material still arriving: VHS captures, ten old communal phones, photographs of old albums, most of it with no EXIF at all - the sixteen defects closed this session were exactly the ones that material would otherwise have hit first.

## What changed recently

- 2026-09-24 **Asking whether the repo had actually learned produced an honest, unflattering answer** (`58cf1ac`). Why: a day of 21 commits, six new tools and sixteen closed defects left `docs/LEARNINGS.md` reading 69, unchanged since morning, because the lessons had been written into STAGE.md rows and test docstrings where only that one stage's reader would find them. Eight rules added (70 to 77), each wired to the code and test that enforces it; the contract test now reads 69 of 77 enforced rather than 61 of 69, and the prose-only debt is unchanged at the same eight rules.
- 2026-09-24 **The mount was authenticated the whole time, and the source archives are gone** (`d38e973`). Why: learning 25 says writing into a Drive mount proves nothing, because the bytes land in a local cache and upload behind it; that was generalised to reading, which is false in the useful direction - a file not in the cache is fetched from Google to satisfy a read, so hashing it is the cross-network check that had been treated as blocked on a token for a whole session. `verify_drive_by_mount.py` matched all 444 sampled files, 0 mismatched, and prints its measured throughput rather than asserting more than a cached read can prove. The four staged Communal-2026-09-22 source zips and their 24,286-file extraction tree are deleted, 162 GB freed, library unchanged at 85,480 files.
- 2026-09-24 **Nobody remembers a file, so the library now answers in events, not just photographs** (`a9d4da7`). Why: `occasion` is a category - "everyday", "travel", "party" - so grouping on it puts every party since 2008 in one bucket; time is the only signal that separates one wedding from another. An event is a run of photographs with no gap over 14 hours, and nothing else; place, people and occasion then describe the event time has already found. 1,388 events over 46,625 photographs, with 37,296 clockless files in no event and every answer saying so.
- 2026-09-24 **The library can be asked in words now** (`40cd00c`). Why: `search` is FTS5 over 15 fields and matches spelling, so it cannot answer a question whose words are not in the data though the description already says exactly that. 85,368 descriptions embedded, 24 minutes, $0.90 measured, 0 failed; each shard is one `.npz` holding its own hashes and vectors, written once and renamed atomically, so a killed run loses one shard rather than corrupting a shared cache the way learning 45's positional pairing once did.
- 2026-09-24 **The index described 94.5% of a library it could not fully see, and correcting it found a second, opposite error** (`80def2c`). Why: `resolve()` had been resolving every hash in the enrichment store whether or not the library still held the file, so 7,172 orphan hashes inflated coverage past 100%; scoping to live hashes dropped `description` to a true 86.1%, and the gap was almost entirely one thing - the whole 2026-09-22 batch had been classified without `--rich` and carried no description at all. The rich pass has since run: description, activity, occasion, mood and subject are all 99.9%.
- 2026-09-24 **An append-only journal read as a set cannot be appended to** (`ceaaf70`). Why: `build_game.py` decided what to ask by checking "does this cluster have any answer" and excluding it if so, which makes a chronological record behave like a set - a later correction routing 57 declined rows to Bharti's game would have looked applied and done nothing. Fixed so the last answer for a merge group decides; 57 rerouted, her queue now 1,192 clusters.
- 2026-09-24 **The naming seams on the new material are worked out, and Bharti's queue is no longer empty** (`8b31235`). Why: three gated rounds on 82 Communal photographs and 117 Personal videos added 1,117 answers and 12 new people, ending by measurement (round 29 built, timed, and withheld at 29 minutes for 58 videos) rather than by running out. The same session corrected a 212.5 GB undecodable-file figure to 9.7 GB once checked against disk rather than a log that still counted deleted files.
- 2026-09-24 **The identity-document check could only fail on what something else had already found** (`f67166d`). Why: it read a one-row list and asked only whether those named files remained, so it passed every session while real identity documents sat unflagged. The file's own comment already named this exact failure, about a different check. Fixed to sweep the chronology by filename; proved able to fail before being trusted, then moved eleven documents.
- 2026-09-23 **The video assigner worked once, then would have silently renamed people on a second run** (`ba0d821`). Why: `assign_video_faces.py` recomputed every video-only cluster from scratch, so a second run would have given the same people different ids; fixed to read the previous assignment back and honour it. Result: 57,565 video faces placed, 5,172 videos now carrying a named person.
- 2026-09-23 **The video assigner could not run at all, and its own test had been failing on exactly that** (`234065b`). Why: it built its frozen centroids from a positional cache that does not exist on this machine; both assigners now join on `(hash, face_index)` instead, closing the same hazard learning 45 was paid for.
- 2026-09-23 **A new cluster id landed on top of one the video assigner already owned, and six carried one of Krish's answers** (`37a3dc9`). Why: two allocators shared no floor. Repaired from backup, re-run onto a floor that is now a maximum across every allocator.
- 2026-09-23 **The frozen-cluster rule had a hole: nothing existed for faces arriving from new photographs**, which is what every ingest produces (`3755998`). `assign_new_faces.py` closes it: 26,646 faces assigned, 7,754 inheriting a name Krish had already given.
- 2026-09-22 **The four Communal Takeout archives are in, and the upload queue that carried them drained to zero** (`a9833bb`, `02113a4`). 163 GB, Communal 12,134 -> 23,841, 0 failed, 0 undated, refused on whole-file hash alone.
- 2026-09-22 **Nothing in the documented ingest chain computes a content hash, and it had been true since the chain was written** (`00beed1`). A row with no hash is invisible, not merely incomplete; `hash_new_files.py` closes it.
- 2026-09-22 **`reconcile_disk.py --write` dated nothing and called every video a photograph** (`a5286ed`). Both are now derived from the path; a third apparent defect turned out to be correct by design and was changed back after being changed once.
- 2026-09-22 **A proposal whose `Side` column was misspelled moved all 11,707 files correctly, then reported every one as a failure** (`f673aa1`). The spelling is now refused at load, before anything moves.
- 2026-09-22 **The 2026-09-21 flatten left the mirror journal and four placement writers describing a layout that no longer existed** (`56800cc`, `47d04a9`). 66,113 journal rows repointed rather than re-sent; one owner now builds the dated path.
- 2026-09-21 **The entry point described yesterday's library, caught by checking rather than asserting** (`ad96733`). `RESUME.md` and `docs/ROADMAP.md` both still quoted the pre-restructure figures in their current-state text; corrected and labelled by date.
- 2026-09-21 **The chronology is one level deep, and both copies match file for file** (`c25e67a`). 7,678 files / 11.28 GB removed to match a mirror-side deletion, 66,047 files moved on each copy, zero failures; three learnings (67 to 69) came out of building the tools for it.
- 2026-09-20 **A safeguard that only refuses at the last step is a race, not a safeguard, and it was closed** (`370c620`). The no-reingest list is now read before a byte is copied, not after.
- 2026-09-20 **The library was deduped against itself, and Drive was found to have quietly drifted from it** (`47856b0`, `7fdc27a`, `eb7adb8`). 1,236 byte-identical internal copies removed; the mirror's own drift closed without re-hashing, since identity was already proven twice over.
- 2026-09-20 **The purge is complete: 1,094.3 GB reclaimed, zero refused, zero skipped as cloud-unproven** (`232476c`, `e68a59c`). A third proof beyond the guard's own re-hash was added first, since deleting a source leaves D: as the only local copy.
- 2026-09-19 **All four remaining sources were audited exactly against the library, then narrowed from 6,350 files to the 669 that actually matter** (`b006479`, `923ca9f`, `375442c`). 88 of those 669 were refused by the no-reingest list on ingest.
- 2026-09-19 **The cloud copy was proven against Google's own checksum, after two confidently wrong readings on the way** (`87640f1`). 0 missing, 0 mismatched, across all 82,100 journalled files.
- 2026-09-19 **A mirror throttle that looked like patience was arithmetic nobody had done** (`c70866c`). Fixed to a checkable rule, pinned by a test (learning 62).

## What is next and what is waiting on Krish

- Waiting on Krish: `gcloud auth login --enable-gdrive-access`, then emptying Drive's trash - about 31 GB of past deletions sits there for 30 days and still counts against quota. The same reauth also unblocks the stronger, server-side `verify_drive_md5.py --refresh`; the mount-based check in `verify_drive_by_mount.py` is real evidence but not a substitute for Google's own checksum. Neither Google account's token can be refreshed from a script, so this needs a human at the keyboard.
- Waiting on Krish: a judgement call on 294 files / 1.29 GB in a video-project folder (render variants and background plates) and a podcast recording - personal, but production assets rather than camera originals, deliberately not decided by the tooling.
- Waiting on Krish: turning on the Windows USN change journal (`fsutil usn createjournal m=32M a=8M D:`) needs an elevated shell and changes a system facility, so it is left for him rather than run unattended - seven files vanished once with no record of the deletion anywhere, and the next time the answer should come from a log rather than an investigation.
- Waiting on Krish (`RESUME.md`, "Waiting on the user"): the origins in `Archive\99-Unsorted\`; `_Review\`'s remaining files awaiting review.
- Settled, no longer waiting: the identity documents that used to sit unflagged in the chronology are found and moved (eleven, `f67166d`); the four staged source zips are deleted, verified by the mount-based check (`d38e973`).
- Next, per `docs/ROADMAP.md`: **the consolidation of scattered sources is finished** - the library is 85,480 files / 926.5 GB on both D: and H: with zero internal duplicates, a chronology one level deep, and it can now be searched and grouped into events. Phase B is the front line: three sources are still arriving, VHS captures, ten old communal phones, and photographs of old albums - two of the three carry no EXIF at all, so naming the folders with their year before ingesting is the single highest-value minute available. This session closed sixteen defects the same shape as ones those sources would otherwise have hit first (learnings 70 to 77), and wrote all of them down this time.
- Still open: Bharti's naming game itself (the mobile app; her data queue, 1,192 clusters, already exists and is not the same thing); whether `_Review` is ever emptied and by what rule; Phase D (a second local copy on E:, not started) and Phase E's onward plan once Phase B adds more to mirror.

## Read next

0. [`docs/ROADMAP.md`](docs/ROADMAP.md): Phases A-F from here to two mirrored libraries and an app - read it for the order, since three dependencies fix it and getting them wrong costs a re-upload.
1. `docs/PRODUCT.md`: what would survive as a product - the reusable core, and what stays a one-off analysis rather than library code.
2. `RESUME.md`: where the work is paused, what to do next, and which decisions are closed. Start here, but see "Do not trust" for what it predates.
3. `state/STATE.json` and `state/PROGRESS.md`: generated by `tools/track.py`, freshest at 2026-09-24T12:07:44; counts the dated chronology only, not `Archive\`/`_Review\`/`ContentProduction\` - see "Where it is right now" for the full-library figures.
4. `guards/STAGE.md` and `stages/*/STAGE.md`: what each of the thirteen stages plus the guards layer owns - inputs, outputs, invariants, code and the learnings each is responsible for.
5. `docs/HANDOVER.md`: what has been consolidated and the data-loss record. Its 2026-09-06 tables predate three restructures since; read `RESUME.md` and this file for what is current.
6. `docs/LEARNINGS.md`: 77 numbered rules, each with its incident. Read before changing any exclusion, deletion or classification rule.
7. `guards/paths.py`: the current library layout.
8. `stages/05_enrich/README.md`: the enrichment engine: metadata, a cheap vision model, a sensitivity pass, then a human.
9. `docs/ARCHITECTURE.md`: the storage model and the canon protocol for sessions; its layout diagram predates the 2026-09-08 restructure.
10. `docs/ORGANISING.md`: the schema and retention rubric, written to be applied to every drive in turn.
11. `docs/MIRROR.md`: how D: and H: end up holding the same library; superseded on the cloud target's size and account by `docs/ROADMAP.md` Phase E.
12. `docs/SEGMENTATION.md`: the segmentation decisions as they stood before the device-level split; the applied rule is in `stages/09_segment/STAGE.md`.
13. `docs/rescues/`: one dated record per source; `2026-09-06-wd6400.md` carries its post-publication correction.
14. `docs/MACHINE-HYGIENE.md`: bring a Windows machine to the agreed clean state.
15. `AGENTS.md`: entry file for coding agents, carrying the fleet's shared canon block.
16. `docs/history/LOG.md`: what the steward did and when.

## Do not trust

- **Any `state/PROGRESS.md` or `state/STATE.json` figure stamped before 2026-09-11T11:31.** `tools/track.py` walked directories the 2026-09-08 restructure had already moved one level deeper under `Media\`, so those runs read the library as **zero** under a header promising every figure is counted from disk. Fixed 2026-09-11 (learning 34).
- **`state/PROGRESS.md` generated at 2026-09-24T12:07:44 predates this session's later commits** - the eleven identity documents moved out of the chronology and the naming rounds that followed. It is regenerated by `tools/track.py` on the library machine, not by the steward; treat its file count as a few hours stale rather than wrong.
- `docs/ARCHITECTURE.md`, `README.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md`: all still describe the pre-restructure `D:\PhotoLibrary` tree and, for segmentation, the pre-device-level split rule. `guards/paths.py` and `stages/09_segment/STAGE.md` are the current record.
- `docs/HANDOVER.md`'s tables ("What has been consolidated", "Library layout as of 2026-09-06"): stamped "Last verified: 2026-09-06", they predate the restructure, the enrichment engine, the segmentation split and the intimate purge. The tables themselves were never re-verified against disk.
- **Any drive letter in a document older than 2026-09-11.** They moved that day: the library drive was `D:` only until the migration finished, and the new 4,657 GB LaCie has held that letter since.
- Any free-space, sync-state or throughput figure not measured in the current session, per `RESUME.md`, "FIRST, ALWAYS: measure the link". H:'s reported free space is the local cache volume, not the Drive account (learning 17).
- A flat DriveFS operations-queue count is not a stall - it can be a stale snapshot missing its WAL file. Compare row identity across two reads, not the count alone.
- `docs/ROADMAP.md`'s "Where this actually is" section and any figure predating the 2026-09-22 Communal ingest or the 2026-09-24 archive deletion: the true current figures are 85,480 files / 926.5 GB on both D: and H:, zero duplicate content, source archives already removed.
