---
repo: krishanraja/contentarchives
product: contentarchives
as_of: 2026-09-20
head: 370c620
lifecycle: building
production_url: none
state_doc: RESUME.md
history_log: docs/history/LOG.md
truth_files: []
authority_order: [state/STATE.json (generated by tools/track.py and counted from disk), state/PROGRESS.md (generated and the same in prose), state/origin-folders.csv (generated by tools/origin_map.py), RESUME.md, docs/HANDOVER.md, docs/LEARNINGS.md, docs/ARCHITECTURE.md, docs/ORGANISING.md, README.md]
steward: https://github.com/krishanraja/control-center/blob/main/docs/steward/RUNBOOK.md
never_publish: [family names and personal folder names, identity or medical or financial documents (the flagged files and the documents named in the rescue record), local paths carrying a user name, drive labels or models or serials (including the one in the rescue record's filename), machine names (including the from-<name> source-set labels in docs/MIRROR.md and RESUME.md), any person's name or profile name appearing in the docs (ORGANISING.md folder examples and the rescue record) and the PERSON-x pseudonyms, cloud account ids and Drive folder paths, descriptions of the intimate or sensitive content itself]
---
# contentarchives: where it is right now

## What it is

contentarchives is a Python toolkit for consolidating scattered personal media (phones, old laptops, external drives, cloud exports, digitised tapes) into one deduplicated, date-organised library without destroying anything on the way, and it is also the canon of the in-flight consolidation it was extracted from: several sessions on more than one machine read and write this repo as the shared record of where that work stands. The toolkit's safety, dating and dedupe layers are tested and in use; an enrichment engine (`stages/05_enrich/`) adds a cheap vision model, a narrow sensitivity pass and a human swipe game for the files metadata alone cannot classify. Its one rule is the README's: "A path is never sufficient grounds to delete." Deletion is allowlist-only and camera-original filenames are protected unconditionally; one deliberate exception now exists, a content Krish personally named for permanent destruction, built with its own journal-first, re-hash-at-delete guards rather than a shortcut around the existing allowlist. The conveyor spans thirteen stages plus `guards/`: 143 of 143 code files owned, 66 learnings, 60 enforced by a named function or test, 6 still enforced only in prose (8, 13, 17, 24, 29, 65) (`tests/test_stage_contracts.py`, run 2026-09-20). The consolidation itself is now finished: the library holds 81,449 files / 805.1 GB with zero duplicate content, verified in two independent places, D: and Google Drive, against Google's own checksums, with 1,114 GB reclaimed from four redundant sources this week.

## Who it is for and why it matters for Mindmake

Nothing here is sold. It is a personal-operations engine built by one person with AI, whose safety rules were paid for with permanently lost files and written down with the incident behind each one (`docs/LEARNINGS.md`: "Every rule here was paid for"). For the room_face buyer in `control-center/docs/ICP.md`, a senior leader at a PE or VC backed media, adtech, publishing or data business, this repo proves two things: that one person can run an unforgiving data job on AI agents across machines without losing control of it, and that the discipline for doing so is written down in public with the failures left in.

Stories a writer can carry without asking Krish:

- **The one truly irreversible action gets more guardrails, not fewer.** When Krish said "purge all intimate content forever," the toolkit could not reuse its own allowlist-deletion path, which refuses unless a surviving copy is proven - and there is deliberately no surviving copy of this. It got its own pair of guards instead: every victim named individually with no globs or path substrings (the same class of rule that destroyed 45 files here once already), a re-hash of each file at the instant of deletion, and a journal written first. Even so, a first pass left traces behind - 7 thumbnails, 5 face vectors, 117 tag rows - because it swept by the files it could still delete rather than by the content it was told to destroy; a second pass closed it, verified from the filesystem, never from the tool's own report (`71210d1`).
- **A guard that fires on a schedule can miss the event that matters.** The first real trial of the cloud-mirror uploader picked the six largest files in the library to test with, including one 101 GB file of pure null bytes already identified as a failed copy - and the throttle meant to stop a runaway upload checked every 25th file, so those six never tripped it. 81 GB of zeros reached the cloud before it was stopped by hand. Fixed to check before every file over 2 GB, a byte budget rather than a file count (`65a9103`).
- **A guard can be watching and still unable to say no.** The mirror's stop-on-failure gate logged its own findings through a helper that piped the same text into its return value, so `[bool]` of a non-empty array read as true no matter what the gate actually decided - a step built specifically to stop the run on a partial upload said "failing on purpose so the task restarts," returned false, and was read as success, leaving 172.5 GB unsent while the task sat idle overnight. The same shape meant a separate verification gate could not have failed from the day it was written, whatever it found. Fixed so the run's answer is the gate's *last* emission, not whatever it happened to print along the way (`c4df90e`, learning 58).
- **A tool can break silently and stay broken for days.** `tools/refresh.py`, the first command this repo tells a resuming session to run, had been failing at exit 2 since a stage migration renamed the script it called - two days before anyone noticed, during which `state/PROGRESS.md` told every reader the library held 73,821 files while the index already held 82,112. Fixed to take repo-relative paths and refuse a missing step loudly rather than silently omitting it (`1d2bb9c`).
- **Documenting is not doing.** `RESUME.md`: "a schema was written to this repo and then described as complete when only the documentation had been produced ... Documenting a structure and applying it are different acts, and only one of them is visible on disk." Every session now starts with an audit that checks the previous session's claims against the filesystem.
- **Never describe state from memory.** `docs/ARCHITECTURE.md`: "a narrated summary drifts from reality within minutes, and the drift is invisible." State is counted from disk by `tools/track.py` and committed; the prose points at it.
- **A convincing number is not a true one, and it keeps recurring.** One session corrected the same measurement three times in a row: 844 named clusters were first reported with an unanswered sibling covering 22,763 photographs, corrected to 463 siblings / 6,180 photographs once double-counted pairs were deduplicated, then corrected again to a true incremental gain of 39 photographs once it was shown that a sibling's reach is almost always already covered by its named partner (`2e12d5b`, `5ebc787`, `bf10c35`).
- **A measured table, not a guess, settles where to draw a line.** Asked to tighten a floor on a naming game, the session swept six candidate settings and showed Krish the one number that actually mattered: the share of people he had already successfully named that a tighter floor would stop showing him. He chose 0.12 with that table in front of him, not a felt number (`cf0ddaf`).
- **Let the agent refuse.** Rule 13: a second machine running the same tooling refused two instructions and was right both times.
- **A green report is not proof, and it keeps recurring.** Rule 23: a copy job reported "0 failures, and it was telling the truth" while later writes destroyed earlier ones. The same shape returned again on 2026-09-16, when `check_repeats.py`'s baseline read zero earlier sheets and printed "CLEAN, every row is new" for a page about to be sent to Krish (`75bb274`).
- **A safeguard that only refuses at the last step is a race, not a safeguard.** Staging 669 irreplaceable files for ingest wrote 88 purged intimate files back onto disk, because the blocklist was only checked later, at ingest itself, and that check refused all 88 before any entered the library. It worked this time. "Recreating destroyed content and relying on a later stage to refuse it is not a safeguard, it is a race that happened to be won" - fixed so the no-reingest list is read before a byte is copied, not after (`370c620`).

Objection it answers: "AI agents will quietly wreck our data." Here is the record of one doing exactly that, once, and the rules that made it unrepeatable, plus the newer record of one catching its own mistakes, and its own blind spots, before either became permanent - including, this week, the one mistake with no possible undo.

## Where it is right now (as of 2026-09-20)

Lifecycle: building.

- **The chronology is split Personal/Communal, device by device.** `propose_split_by_path.py` / `review_split_by_path.py` / `apply_split_by_path.py` replaced two scripts marked broken by measurement, and Krish judged 155 camera+year groups rather than 18,985 individual rows, naming 13 device models and 3 no-camera years as Communal. Applied: 18,934 files moved (8,372 Communal, 10,613 Personal), verified from the filesystem afterwards; 138 filename collisions between different photographs were renamed rather than overwritten, 49 byte-identical duplicates skipped rather than deleted (`72ad697`, `23d4568`, `377b7e4`).
- **Intimate content is isolated, then permanently destroyed, on Krish's word, and the purge is now enforced forever rather than for a day.** 88 files were moved into `Media\Personal\Intimate`, flattened to one folder, then destroyed with journal-first, re-hash-at-delete guards. The two files two model passes disagreed about are settled: they stay, by Krish's decision, permanently and on purpose (`7fdcf60`). `PURGED-HASHES.csv` is now read by every ingest path - two directly, two by delegation - so a purged hash cannot be re-admitted by a future phone or archive; the list's own first write had corrupted itself, a size in the Hash column, found and fixed by identifying a hash by its 64-hex shape rather than by column position (`d512cfe`, `a39fddf`, `d7ad348`, learnings 57 and 60).
- **A slice of the library was invisible to every enrichment pass at once**, because the thumbnail chain only ever ran against `Media\`; 2,426 files in `Archive\`, `_Review\` and `ContentProduction\` had no thumbnails and were therefore blind to the classifier, the descriptions, the face pass and the naming game simultaneously. Closed (`backfill_thumbs.py`); a targeted sensitivity pass then bounded uncensored-nudity risk in the 77,755 already-clean files at under 0.23%.
- **The naming rounds are done by design: 23 rounds, roughly 1,433 answers, 280 people.** The largest unnamed cluster fell below the 8-photograph floor Krish chose, so `people_sheet.py` now prints THE NAMING ROUNDS ARE DONE rather than a short page (`2e12d5b`).
- **A phone swipe game replaces the sheet for what is left, and it round-trips unattended.** Answers land in a Claude Artifact's shared database and a scheduled chain reads them back into the journal, so the loop survives the session closing. Four batches done, 102 answers ingested for Krish; Bharti's own queue (~31 batches) is defined but not yet built. The face-size floor is 0.12 of the frame, chosen from a measured six-point table (`a4fb25f` .. `bf62200`, `cf0ddaf`).
- **`audience` decides who may see a photograph, recomputed on every build**: `family` when a photograph holds someone who also appears in Communal, `private` otherwise - deliberately the inverse of the segmentation rule, since a photograph *of* someone is not the same signal as a photograph taken *for* their folder (`0a21b6f`).
- **The front door was broken for two days.** `tools/refresh.py` had been failing at exit 2 since the 2026-09-15 stage migration; fixed, and `docs/ROADMAP.md` was rewritten wholesale from fresh measurement in the same commit, replacing a week-stale version and its Phase A-F plan is now the current one (`1d2bb9c`).
- **The H: mirror finished and was proven, not assumed.** `verify_drive_md5.py` matched all 82,100 journalled files against Google's own server-side `md5Checksum` - 0 missing, 0 mismatched - after two confident wrong answers on the way: a path-key bug that first reported all 82,100 as missing, and a basename fallback that would have quietly confirmed a file sitting in the wrong folder rather than flagging it (`87640f1`). A throttle that had stalled three files from the end turned out to be arithmetic, not a slow cloud: the floor plus the largest permitted file could never fit in the volume's real headroom, only in the number nobody had checked (`c70866c`, learning 62).
- **All four remaining external sources were audited exactly against the library, then narrowed to what actually needs saving.** E:, G:, H:'s `_photo-consolidation` and OneDrive hold 108,628 files / 1,094.3 GB the library already proves it has, byte for byte, and every source reconciled exactly against an independent walk. Of the 6,350 files that exist only outside the library, narrowing by content rather than by row count found 5,067 were screenshots, build output and thumbnail caches, leaving 669 irreplaceable camera originals to ingest before anything is purged (`b006479`, `923ca9f`, learning 63). OneDrive's own figure, quoted for days at 25,782 files / 140.7 GB, was a snapshot of a mid-sync filesystem; three later measurements agree on 1,099 files / 1.1 GB, wrong by more than a hundredfold (`85ccf7c`, learning 65).
- **The purge is complete: 1,094.3 GB reclaimed, zero files refused, zero skipped as cloud-unproven.** Before purging, the 669 camera originals were staged and ingested - 88 of them refused by the no-reingest list, because they were the purged intimate files whose copies had outlived the purge on E: and H: (`375442c`, learning 64). Deleting E: leaves D: as the only local copy, so `purge_audited_sources.py` needed a third proof beyond its own re-hash: the survivor's md5 has to appear in Google's own Drive listing, not merely in the library (`e68a59c`). E:'s free space went 128 to 873.3 GB (`232476c`).
- **The library was then deduped against itself, and the cloud copy was brought back into step.** 1,236 files inside the library were byte-identical to another file in it - every one of the 1,205 groups checked for a shared inode first, so the 20.07 GB removed was real, not two names for one file - kept by a written keeper rule (curated folders before working copies, camera-native names before app exports) rather than an unargued tiebreak (`47856b0`, `7fdc27a`). The mirror only ever adds, so Drive silently held 1,236 files the library no longer did until `unmirror_deleted.py` removed them without re-hashing - identity was already proven twice, by the dedupe's own re-hash and by Google's checksum - refusing any victim whose keeper is missing from Drive or whose size has drifted (`eb7adb8`, learning 66). **The library is now 81,449 files / 805.1 GB with zero duplicate content, and D: and Drive match again.**
- **Nothing still to run needs the Anthropic API**, which Krish has turned off; classification, faces, verifiers, `build_db` and the video pipeline are all Gemini or local (`f7d1658`, still holding).
- **The repository stays public**, Krish's decision on 2026-09-15: commit messages and docs may name family members next to photograph counts, but the answers journal, face data and anything the publication tripwire catches stay off GitHub.
- **Standing approval to push to `main` by default**, given by Krish on 2026-09-11 and recorded in `RESUME.md`.
- **Largest open risk, updated**: no longer the mirror or an unproven purge - both are done and verified. Two things need Krish directly: `gcloud auth login --enable-gdrive-access` then emptying Drive's trash, since roughly 31 GB of deleted-but-not-yet-purged bytes still counts against quota for 30 days, and a judgement call on 294 files / 1.29 GB of finished video-project renders and a podcast recording that are personal but not camera originals. Otherwise the front line is new material arriving over the coming weeks: VHS captures, ten old communal phones, and photographs of old albums, most of it with no EXIF at all (`370c620`).

## What changed recently

- 2026-09-20 **A safeguard that only refuses at the last step is a race, not a safeguard, and it was closed** (`370c620`). Why: staging 669 irreplaceable files for ingest wrote 88 purged intimate files back onto disk before the blocklist got a chance to refuse them at ingest itself; it refused all 88 this time, but "recreating destroyed content and relying on a later stage to refuse it is not a safeguard, it is a race that happened to be won." The no-reingest list is now read before a byte is copied. Five learnings (62 to 66) closed out the session in the same commit, and the debt count moved from 5 to 6 prose-only rather than being hidden behind a fake enforcement (learning 65).
- 2026-09-20 **The library was deduped against itself, and Drive was found to have quietly drifted from it** (`47856b0`, `7fdc27a`, `eb7adb8`). Why: 1,236 files inside the library were byte-identical to another file already in it - every group checked for a shared inode first, so the 20.07 GB removed was real - kept by a written keeper rule rather than an arguable tiebreak. A mirror that only ever adds cannot notice a source that shrinks: every check stayed green while D: held 81,449 files and Drive still held 82,685. `unmirror_deleted.py` closed the gap without re-hashing, since identity was already proven twice over, refusing any victim whose keeper is missing from Drive or whose size has moved (learning 66). The library is now 81,449 files / 805.1 GB, zero duplicate content, both copies matching.
- 2026-09-20 **The purge is complete: 1,094.3 GB reclaimed, zero refused, zero skipped as cloud-unproven** (`232476c`, `e68a59c`). Why: deleting a source when D: is the only remaining local copy needed a third proof beyond the guard's own re-hash - the survivor's md5 has to appear in Google's own Drive listing, not merely in the library - added to `purge_audited_sources.py` before the 108,628-file purge ran. E:'s free space went 128 to 873.3 GB.
- 2026-09-19 **All four remaining sources were audited exactly against the library, then narrowed from 6,350 files to the 669 that actually matter** (`b006479`, `923ca9f`, `375442c`). Why: a `UNIQUE` verdict means the library lacks those bytes, not that anyone would miss them - grouping candidates by content rather than counting rows found 5,067 were screenshots and build output, leaving 669 camera originals to ingest first. 88 of those 669 were refused by the no-reingest list on ingest: purged intimate files whose copies had outlived the purge on E: and H: (learnings 63 and 64).
- 2026-09-19 **The cloud copy was proven against Google's own checksum, after two confidently wrong readings on the way** (`87640f1`). Why: the first verification run said all 82,100 files were missing, because Drive's `files.list` never returns the account root and the reconstructed path started one folder short; a basename fallback would then have quietly confirmed a file sitting in the wrong folder instead of flagging it. Fixed to key paths correctly and to fail loudly as `UNKEYED` rather than falling back. Result: 0 missing, 0 mismatched, 0 without a checksum, across all 82,100 journalled files.
- 2026-09-19 **A mirror throttle that looked like patience was arithmetic nobody had done** (`c70866c`). Why: the mirror sat logging "waiting for room" every minute on the last three files, killed and restarted by the supervisor each time, because a 40 GB floor plus a 20 GB largest-file cap could never fit in a volume whose real steady-state free space was 45.7 GB - not slowly, never. The same session had rewritten the neighbouring constant an hour earlier for the identical reason without checking this one. Fixed to a checkable rule, `CACHE_FLOOR_GB + MAX_FILE_GB <= measured headroom`, pinned by a test (learning 62).
- 2026-09-19 **RESUME.md's OneDrive figure was wrong by more than a hundredfold, because the filesystem it measured was mid-sync** (`85ccf7c`). Why: 25,782 files / 140.7 GB had been quoted as a headline number; three later measurements agree on 1,099 files / 1.1 GB. The tool that disagreed was doubted first, because it contradicted a number already published, costing three round trips debugging the messenger before the real cause - a syncing filesystem measured and then quoted as a fact - was found (learning 65).
- 2026-09-18 **The purged-content blocklist went from written to enforced, then from broken to fixed** (`d512cfe`, `a39fddf`, `d7ad348`). Why: "purge all intimate content forever" destroyed 88 hashes the day before, and nothing read the resulting no-reingest list - a purge that only unlinks files is a purge with a time limit. `is_blocked()` now refuses a purged hash by content in all four ingest paths, two direct and two by delegation, found only by reading the delegation rather than assuming, gated on size so 88 hashes cost no throughput on the rest of the library. The list's own first write had corrupted itself, a size landed in the Hash column, found because a hash is now identified by its 64-hex shape rather than by column position; the same seven rows were already covered by the wider purge, so nothing re-admitted was actually at risk (learning 60).
- 2026-09-18 **A purge summary counted one set and acted on another** (`6f1ab47`). Why: the count a human approves a deletion from was computed over targets-plus-blocked-hashes while the three actions that do the deleting keyed on targets alone, so the printed 117 tag rows, 5 face vectors and 5 bounding boxes overstated what would actually go - the more dangerous direction, since "I already dealt with that" is exactly the belief that must not be false about purged intimate content. Fixed at all three action sites; a `--traces-only` mode added for sweeping derived copies of already-gone hashes without touching a file (learning 61).
- 2026-09-18 **The mirror's own logging helper meant its stop-on-failure gate could never fail, and a long-path prefix broke silently for months** (`c4df90e`). Why: `Say` piped through `Tee-Object`, which also writes to the pipeline's output, so every log line inside a gate became part of that gate's return value and `[bool]` of a non-empty array is true - the mirror's Postcondition said "failing on purpose so the task restarts," returned false, and was read as success, leaving 172.5 GB unsent overnight while the task sat idle; the same shape meant a `-Verify` step could never fail from the day it was written. Fixed by having the runner take a gate's last emitted value rather than trusting a call site not to log inside one (learning 58). Found while chasing it: `lp()`'s long-path prefix breaks on the Drive mount - `os.path.exists` answers false for a file plainly there - hidden until now because only re-writes hit it and 82,055 new files never did (learning 59).
- 2026-09-18 **Four more traps found running the mirror overnight, each looked like working machinery** (`2b7cdeb`, `27b7aac`, `1a741d3`, `29b4d74`). Why: an ETA compared 27 files left against the whole 82,112-file library and printed "249,513 minutes"; a queue-depth check copied a snapshot missing its WAL file and read a stale count, sitting frozen at 630 GB while Google would happily take more; a clean exit on a throttle stop read as success to Task Scheduler, so a 781-minute run that stopped with 497 GB unsent never restarted; and the last 27 outstanding files, all over 2.7 GB, deferred forever because the per-file headroom check gave up rather than waiting for the cache to drain, which it does on its own at the upload's own pace. Fixed: the step now estimates from what the run will actually do, the queue read copies the WAL, a throttle stop returns non-zero, and `wait_for_headroom` waits for room for the specific file rather than a general figure.
- 2026-09-18 **A verification tool lost an entire run to the exact failure mode its own neighbour had just been rewritten to survive** (`c7ba33b`). Why: the 278-video hash check was killed for low memory - two Drive downloads running at once - having held every verdict in memory before writing its CSV once at the end, the same design `mirror_to_h.py`'s append-only, fsync-per-file journal had been built an hour earlier specifically to avoid; rewritten to match. A deletion was then found to be as invisible to the path-keyed inventory as a move: a rebuild reads `INVENTORY.csv` and the hash maps, not the disk, so the 101 GB null-byte file's row had to be dropped by hand before a rebuild would agree with reality (`59b6b5c`).
- 2026-09-18 **All four things blocking the mirror got measured and closed in one session** (`7fdcf60`). Why: the Drive login had been blamed for hours, but the real fault was the active `gcloud` account pointing at the wrong Google account; switching it answered the real quota immediately (2,048 GB limit, 1,463.6 GB free, matching Krish's own figure). The 101 GB null-byte file was deleted from both D: and E: after its first 1 MB and last 64 KB were re-read as zero at the instant of deletion, freeing 202.2 GB. 283 videos flagged as possibly-unique turned out to be 278 WhatsApp copies of files already held under their clean names, checked by size rather than assumed; five 1,241-byte DJI stub files with no matching clip anywhere in the library were found to be the only surviving evidence that part of a wedding shoot never transferred, and marked never to be deleted.
- 2026-09-18 **The H: mirror is armed and running, and its consumed sources are cleared** (`87e9f73`). Why: 22,103 files / 461.7 GB were deleted from the H: staging source with 0 failures once proven held in the library, keeping 2,798 files - mostly video variants and proxies - that could not be proven; Drive's own trash holds all of it for 30 days regardless. Measured rather than assumed: 824.2 GB / 82,111 files left to mirror at 44.7 MB/s to the local cache, bounded by upload rather than by this machine. A `--band` flag that argparse declared and nothing read had silently re-sent the same 40 empty files for the fourth time this session with no behaviour behind the flag; `cache_free_gb()` now takes the minimum of C: and H:, since DriveFS stages every upload through C: regardless of which drive the destination names.
- 2026-09-18 **RESUME.md said the mirror was running with numbers already hours stale, and the progress-counting command it gave the next session undercounted** (`cdc00cc`, `a4e4e34`). Why: the handover claimed 8,579 files journalled and a ~70 hour ETA while the true count was 82,077 of 82,104 files / 688.1 GB with 27 files left, ~4.4 hours bounded by Drive's own upload rate - rewritten from measurement rather than left. The counting command itself then undercounted: a file already on H: at the right size journals `already-present`, not `written`, so counting only `written` rows read like files had gone missing. Both background watchers set up to monitor the run were killed within minutes by this machine's own low-memory watchdog; nothing is lost, since the scheduled task restarts up to 99 times and the writer resumes from its journal, but the run is genuinely unwatched rather than quietly supervised.
- 2026-09-17 **The chronology is split Personal/Communal, device by device, and 18,934 files moved** (`72ad697`, `23d4568`, `377b7e4`). Why: `propose_split_by_path.py` and `apply_split_by_path.py` were both marked broken by measurement - the proposer keyed on origin folder while 59% of the unsided library has none, the applier still targeted a pre-migration path layout. Krish reviewed 155 camera+year groups rather than 18,985 rows, naming 13 device models and 3 no-camera years Communal, five of them covering years he had not separately named because "a phone does not change owner between years." Applied 8,372 files to Communal, 10,613 to Personal; the applier's own first run refused itself correctly when two approved sources turned out to no longer exist, because the inventory had not been refreshed after an earlier move.
- 2026-09-17 **A hash-keyed enrichment silently detaches from a path-keyed inventory on every move, and it did twice** (`995fe8b`, `19697d5`, `4c78810`). Why: after the split moved 18,934 files, the path-to-hash maps enrichment depends on were not repatched, so 19,022 files silently lost description coverage (96.9% to 74.1%) while every other check stayed green. `patch_inventory_moves.py` rewrites moved rows from the move journal rather than rewalking 82,193 files, and gained `--column`/`--headerless`/`--partial` once the two maps turned out to need different treatment.
- 2026-09-17 **Intimate content is isolated, then permanently destroyed, on Krish's word** (`ee1be78`, `b0be142`, `71210d1`). Why: "move everything intimate into a separate folder" (88 files, stage 09's first tests), then "remove the chronology folder structure from Intimate" (flattened to one folder), then "purge all intimate content forever" - destroyed with a purpose-built pair of guards: named victims only, a re-hash at the instant of deletion, and a journal written first. Verified from the filesystem afterwards: 0 files, 0 thumbnails, 0 tag rows, 0 face vectors, 0 bounding boxes across all 88 hashes. Two files two model passes disagree about stay, on purpose, for Krish.
- 2026-09-17 **A slice of the library was invisible to every pass at once, because none of them ever looked** (`b0be142`). Why: the thumbnail chain only ever ran against `Media\`, so 2,426 files in `Archive\`, `_Review\` and `ContentProduction\` had no thumbnails and were blind to the classifier, descriptions, faces and the naming game simultaneously. `backfill_thumbs.py` closed it by driving from the index rather than rewalking the library; a targeted sensitivity pass then asked 4,112 files a narrow nudity question for $2.22 and bounded the risk in the 77,755 already-clean files at under 0.23%.
- 2026-09-17 **The naming rounds are done by design: 23 rounds, roughly 1,433 answers, 280 people** (`2e12d5b`, `5ebc787`, `bf10c35`). Why: the largest unnamed cluster fell below the 8-photograph floor Krish chose. The same session corrected one number three times in a row - 844 clusters with an unanswered sibling covering 22,763 photographs, then 463 siblings / 6,180 photographs once double-counted pairs were deduplicated, then a true incremental gain of 39 photographs once it was shown a sibling's reach is almost always already covered by its named partner. Krish declined a further round on what was left.
- 2026-09-17 **A phone swipe game replaces the sheet, and it now round-trips unattended** (`a4fb25f`, `ffdf651`, `8bdfe58`, `00ee608`, `0814d21`, `a675769`, `bf62200`, `c6f3518`). Why: Krish needs to answer from his phone with no session open, so the page holds its own database of names and pushes answers to a Claude Artifact's shared store, which a scheduled chain reads back into the journal. Getting there cost four real Android bugs found on his own phone: a 290-name `<datalist>` rendering full-screen over the Skip button; auto-focus throwing that list open on arrival; the keyboard shrinking `visualViewport` rather than resizing the window, so the field had to be re-scrolled on resize, not on focus; and a skip that never reached the journal because only named rows were sent, so an entire batch of already-skipped faces came straight back - "if I'm skipping, I don't care that they never end up classified and you need to be ok with that." Four batches done, 102 answers ingested; Bharti's own queue is not yet built.
- 2026-09-17 **The face-size floor for the game is 0.12 of the frame, chosen from a measured table, not a guess** (`e4c5616`, `cf0ddaf`). Why: Krish had said he usually declined a face because the thumbnail was poor; measuring his 485 declined clusters found a median face of 25 source pixels, which looked like confirmation - until a comparison showed him the actual crops, and he corrected it himself: "all those boxes are just people in the background, not really important." A six-point table then showed the tradeoff plainly: at 0.12, 48% of the people he had already successfully named would never have been shown to him. He chose 0.12 with that cost in view.
- 2026-09-17 **`audience` decides who may see a photograph, recomputed on every build** (`0a21b6f`). Why: there had never been a default for the 41,124 Personal files naming nobody - Krish clarified there was no policy decision to make, they are simply not yet named. `family` when a photograph holds someone who also appears in Communal, `private` otherwise, recomputed on every build. Deliberately the inverse of the segmentation rule: a photograph *of* Bharti is not automatically Communal, only a photograph taken *for* her folder is.
- 2026-09-17 **The front door was broken for two days, and the roadmap was a week stale** (`1d2bb9c`). Why: `tools/refresh.py` had been failing at exit 2 since the 2026-09-15 stage migration moved the script it called, resolved by bare name; `state/PROGRESS.md` reported 73,821 files while the index already held 82,112. Fixed to resolve repo-relative paths and refuse a missing step loudly. `docs/ROADMAP.md` was rewritten wholesale from fresh measurement in the same commit, correcting two false assumptions: `E:\ContentLibrary` is a stale predecessor, not a mirror, and `G:` and `H:` are two different Google accounts, not one.
- 2026-09-17 **The cloud mirror target is settled at 2 TB, and a first trial wrote 81 GB of zeros before it was caught** (`e4079d2`, `65a9103`). Why: Krish confirmed H: has 2 TB, a figure this machine cannot verify locally because the mount reports only its local DriveFS cache (learning 17); measured from the mount's own metadata, 1,510 GB is free against a ~925 GB library. The first real trial of `mirror_to_h.py` selected the six largest files to test with, one a 101 GB file of pure null bytes already identified as a failed copy, and a throttle checking every 25th file never fired for six files. Fixed: check before every file over 2 GB, cap any single file at 20 GB (named, never silently skipped), defer anything the remaining cache headroom cannot hold. Excluding the corrupt file, the true library to mirror is 824.2 GB, not 925.3 GB.
- 2026-09-17 **A test suite was overwriting the one file money and compute cannot reproduce, on every run, all week** (`d70edd3`). Why: `record_people.py` held a hardcoded path to the answers-journal backup on a shared drive, and the test suite ran the same recorder against a two-row fixture, copying it over 1,508 real answers each time. Nothing was lost - the local journal was untouched - but the safety net for it was silently gone the whole time. Fixed with an explicit `--backup` argument and a test that measures the live file's size and mtime are unchanged by a suite run, rather than trusting the argument works.

## What is next and what is waiting on Krish

- Waiting on Krish: `gcloud auth login --enable-gdrive-access`, then emptying Drive's trash - about 31 GB of the deletions from the dedupe and the purge sits there for 30 days and still counts against quota. Neither Google account's token can be refreshed from a script, so this genuinely needs a human at the keyboard.
- Waiting on Krish: a judgement call on 294 files / 1.29 GB in `G:\My Drive\Lozzy Mems\Loz_Video_Package` (engagement-video render variants and background plates) and a 508 MB podcast recording - personal, but production assets rather than camera originals, deliberately not decided by the tooling.
- Settled, no longer waiting: the two files two model passes disagreed about stay classified intimate outside the purged folder, permanently and on purpose, by Krish's own decision (`7fdcf60`).
- Waiting on Krish: turning on the Windows USN change journal (`fsutil usn createjournal m=32M a=8M D:`) needs an elevated shell and changes a system facility, so it is left for him rather than run unattended (Phase A5) - seven files vanished on 2026-09-17 with no record of the deletion anywhere, and the next time the answer should come from a log rather than an investigation.
- Waiting on Krish (`RESUME.md`, "Waiting on the user"): the five identity documents still in `Media\NoDate\`; the origins in `Archive\99-Unsorted\`; `_Review\`'s 5,844 files awaiting review.
- Next, per `docs/ROADMAP.md`: **the consolidation itself is finished** - Phase A is done (the blocklist is live and caught its first real re-entry attempt, `--traces-only` exists, `refresh.py` is fixed) and the library is 81,449 files / 805.1 GB with zero internal duplicates and two independently verified copies. Phase B is now the front line: three sources are arriving over the coming weeks, VHS captures, ten old communal phones, and photographs of old albums - two of the three carry no EXIF at all, so naming the folders with their year before ingesting is the single highest-value minute available.
- Still open: Bharti's naming game (~31 batches, not yet built); whether `_Review` is ever emptied and by what rule; Phase D (a second local copy on E:, not started - E: still holds the stale predecessor library) and Phase E's onward plan once Phase B adds more to mirror.

## Read next

0. [`docs/ROADMAP.md`](docs/ROADMAP.md): rewritten 2026-09-17 from measurement, replacing a week-stale version. Phases A-F from here to two mirrored libraries and an app - read it for the order, since three dependencies fix it and getting them wrong costs a re-upload.
1. `docs/PRODUCT.md`: what would survive as a product - the reusable core, and what stays a one-off analysis rather than library code.
2. `RESUME.md`: where the work is paused, what to do next, and which decisions are closed. Start here, but see "Do not trust" for what it predates.
3. `state/STATE.json` and `state/PROGRESS.md`: generated by `tools/track.py`, freshest at 2026-09-17T22:22:48; `state/PROGRESS.md` counts the dated chronology only, not `Archive\`/`_Review\`/`ContentProduction\` - see "Where it is right now" for the full-library figures.
4. `guards/STAGE.md` and `stages/*/STAGE.md`: what each of the thirteen stages plus the guards layer owns - inputs, outputs, invariants, code and the learnings each is responsible for.
5. `docs/HANDOVER.md`: what has been consolidated and the data-loss record. Its 2026-09-06 tables predate three restructures since; read `RESUME.md` and this file for what is current.
6. `docs/LEARNINGS.md`: 66 numbered rules, each with its incident. Read before changing any exclusion, deletion or classification rule.
7. `guards/paths.py`: the current library layout.
8. `stages/05_enrich/README.md`: the enrichment engine: metadata, a cheap vision model, a sensitivity pass, then a human.
9. `docs/ARCHITECTURE.md`: the storage model and the canon protocol for sessions; its layout diagram predates the 2026-09-08 restructure.
10. `docs/ORGANISING.md`: the schema and retention rubric, written to be applied to every drive in turn.
11. `docs/MIRROR.md`: how D: and H: end up holding the same library; superseded on the cloud target's size and account by `docs/ROADMAP.md` Phase E.
12. `docs/SEGMENTATION.md`: the segmentation decisions as they stood before the device-level split; the applied rule is in `stages/09_segment/STAGE.md`.
13. `docs/rescues/`: one dated record per source; `2026-09-06-wd6400.md` carries its post-publication correction.
14. `docs/MACHINE-HYGIENE.md`: bring a Windows machine to the agreed clean state.
15. `AGENTS.md`: entry file for coding agents, carrying the fleet's shared canon block.
16. `docs/history/LOG.md`: what the steward did and when.

## Do not trust

- **Any `state/PROGRESS.md` or `state/STATE.json` figure stamped before 2026-09-11T11:31.** `tools/track.py` walked directories the 2026-09-08 restructure (`d5470d7`) had already moved one level deeper under `Media\`, so those runs read the library as **zero** under a header promising every figure is counted from disk. Fixed 2026-09-11 (learning 34).
- **A green check from `tools/audit_previous_session.py` dated before 2026-09-11.** It had the same bug and two more: it reported a 64,782-file library as 0, reported the Personal/Communal split as never applied while both trees existed, and PASSED its identity-document check by finding nothing in directories that do not exist.
- `docs/ARCHITECTURE.md`, `README.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md`: all still describe the pre-restructure `D:\PhotoLibrary` tree and, for segmentation, the pre-device-level split rule. `guards/paths.py` and `stages/09_segment/STAGE.md` are the current record.
- The duplicate-count history: `state/STATE.json`'s 10,026 rejected and the Takeout merge's 382 rejected were produced by an index later found to cover only 12% of the library, not merely stale (learning 27 blamed staleness; `e4812dc` found the walk itself missed `Personal\` and `Communal\` entirely).
- `docs/HANDOVER.md`'s tables ("What has been consolidated", "Library layout as of 2026-09-06"): stamped "Last verified: 2026-09-06", they predate the restructure, the enrichment engine, the segmentation split and the intimate purge. The tables themselves were never re-verified against disk.
- **Any drive letter in a document older than 2026-09-11.** They moved that day: the library drive was `D:` only until the migration finished, and the new 4,657 GB LaCie has held that letter since.
- Any free-space, sync-state or throughput figure not measured in the current session, per `RESUME.md`, "FIRST, ALWAYS: measure the link". H:'s reported free space is the local cache volume, not the Drive account (learning 17). The upload rate off this machine is now a real sustained measurement, 8.73 MB/s over three minutes with the machine otherwise idle, superseding the earlier stopped-early trial.
- `docs/ROADMAP.md`'s "Where this actually is" section (82,106 files / 925.4 GB, and the field-coverage percentages under it) and `state/PROGRESS.md` (generated 2026-09-17T22:22:48) predate the 101 GB null-byte deletion, the 2026-09-19/20 purge of four redundant sources, and the library's own dedupe. The true current figure is 81,449 files / 805.1 GB, zero duplicate content, in `RESUME.md`'s "RIGHT NOW" section.
- A flat DriveFS operations-queue count is not a stall - it can be a stale snapshot missing its WAL file. Compare row identity across two reads, not the count alone (learning from `27b7aac`, folded into `mirror_to_h.py`'s own queue check).
