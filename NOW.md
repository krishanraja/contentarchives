---
repo: krishanraja/contentarchives
product: contentarchives
as_of: 2026-09-09
head: 77d614e
lifecycle: building
production_url: none
state_doc: RESUME.md
history_log: docs/history/LOG.md
truth_files: []
authority_order: [state/STATE.json (generated by tools/track.py and counted from disk), state/PROGRESS.md (generated and the same in prose), state/origin-folders.csv (generated by tools/origin_map.py), RESUME.md, docs/HANDOVER.md, docs/LEARNINGS.md, docs/ARCHITECTURE.md, docs/ORGANISING.md, README.md]
steward: https://github.com/krishanraja/control-center/blob/main/docs/steward/RUNBOOK.md
never_publish: [family names and personal folder names, identity or medical or financial documents (the flagged files and the documents named in the rescue record), local paths carrying a user name, drive labels or models or serials (including the one in the rescue record's filename), machine names (including the from-<name> source-set labels in docs/MIRROR.md and RESUME.md), any person's name or profile name appearing in the docs (ORGANISING.md folder examples and the rescue record) and the PERSON-x pseudonyms, cloud account ids and Drive folder paths]
---
# contentarchives: where it is right now

## What it is

contentarchives is a Python toolkit for consolidating scattered personal media (phones, old laptops, external drives, cloud exports, digitised tapes) into one deduplicated, date-organised library without destroying anything on the way, and it is also the canon of the in-flight consolidation it was extracted from: several sessions on more than one machine read and write this repo as the shared record of where that work stands. The toolkit's safety, dating and dedupe layers are tested and in use; a new enrichment engine (`engine/`) adds a cheap vision model and a human swipe game for the files metadata alone cannot classify (`engine/README.md`). Its one rule is the README's: "A path is never sufficient grounds to delete." Deletion is allowlist-only, camera-original filenames are protected unconditionally, and a duplicate may only be declared on a whole-file hash match.

## Who it is for and why it matters for Mindmake

Nothing here is sold. It is a personal-operations engine built by one person with AI, whose safety rules were paid for with permanently lost files and written down with the incident behind each one (`docs/LEARNINGS.md`: "Every rule here was paid for"). For the room_face buyer in `control-center/docs/ICP.md`, a senior leader at a PE or VC backed media, adtech, publishing or data business, this repo proves two things: that one person can run an unforgiving data job on AI agents across machines without losing control of it, and that the discipline for doing so is written down in public with the failures left in.

Stories a writer can carry without asking Krish:

- **Documenting is not doing.** `RESUME.md`: "a schema was written to this repo and then described as complete when only the documentation had been produced ... Documenting a structure and applying it are different acts, and only one of them is visible on disk." Every session now starts with an audit that checks the previous session's claims against the filesystem. The buyer's teams pay for this lesson too.
- **Never describe state from memory.** `docs/ARCHITECTURE.md`: "a narrated summary drifts from reality within minutes, and the drift is invisible." State is counted from disk by `tools/track.py` and committed; the prose points at it.
- **The rigour went to the safe operation.** `docs/LEARNINGS.md` rule 1: a content-hash engine was used for adding files and a filename regex for deleting them. "If you have a strong check, use it on the irreversible path first."
- **A green report is not proof, and it keeps recurring.** Rule 23: a copy job reported "0 failures, and it was telling the truth. Every write succeeded" while later writes destroyed earlier ones. Rule 6's addendum: a verifier's fallback branch "reads the first four bytes and records them as a signature. That branch has no failure mode." A zero-byte file passed it twice and shipped, caught only afterwards by counting zero-byte files on the finished result and asking why the answer was not zero (`c3c6a80`).
- **Let the agent refuse.** Rule 13: a second machine running the same tooling refused two instructions and was right both times.
- **A convincing number is not a true one.** `docs/LEARNINGS.md` rule 28: a folder listing said 264 GB was free for the taking, then a hash sample said 96.8% of a set was duplicated, both true by construction, because hardlinked files are 94-97% of that folder and two hardlinks to one inode hash identical by reading the same bytes twice. "The better the earlier work, the more convincing the illusion." A second, subtler version of the same mistake cost 88% of the library's dedup coverage a day later (`e4812dc`).
- **Admitting the call was wrong, with the number attached.** A rules-based sweep had moved 13,446 received files out of the chronology, reasoned as "not photographs you took ... anything precious almost certainly exists elsewhere." A vision pass checked that call against the actual images and found it wrong: "most are genuine photographs of a life, sent by the people in them." 10,004 of 15,689 reviewed files were real memories; 9,975 were restored two days later, verified four separate ways (`8f05203`, `9d118a1`).

Objection it answers: "AI agents will quietly wreck our data." Here is the record of one doing exactly that, once, and the rules that made it unrepeatable, plus the newer record of one catching its own mistake before it became permanent.

## Where it is right now (as of 2026-09-09)

Lifecycle: building. Working tools extracted from a live consolidation, with the consolidation itself in flight and now mid-restructure. `RESUME.md` is the front door for a session resuming the work; its "Where things stand" section is dated 2026-09-07 and predates everything below (see "Do not trust").

- **Layout**: as of `d5470d7` (2026-09-08) the library lives under one root, `D:\ContentLibrary`, with `Media\{Personal,Communal,NoDate,Pending-Segmentation}` holding the chronology and `Archive\`, `ContentProduction\`, `_Review\` and `_Catalog\` beside it. 81,306 files moved by directory rename, no bytes copied. `scripts/paths.py` is now the single place the layout is defined.
- **Whole-library count**: 68,042 files, from an on-disk inventory rebuilt at `77d614e` (2026-09-09) after a doubled root had inflated the first rebuild to 70,056. `state/STATE.json`'s own library counts read zero and are not a source for this figure (see "Do not trust").
- **Chronology after the restore**: `_Review` (judged not-a-memory, nothing deleted) fell from 29,092 to 5,844 files, and `Personal` grew from 41,965 to 51,900, when a vision pass's 10,004 keep=yes verdicts were restored, less 29 left behind because their origin was never journalled (`9d118a1`).
- **Enrichment**: the new engine (`engine/`) classified 15,689 `_Review` files by metadata, then Haiku, for $11.72 in 65 minutes with zero failures (`8f05203`); it is general-purpose and not specific to this library (`engine/README.md`).
- **Ingestion** (`state/STATE.json`, generated 2026-09-09T06:45:19, unaffected by the layout-count bug below): 60,398 files added, 10,026 duplicates rejected on content hash (an undercount, see "Do not trust"), 2,363 files rescued off the failing E: drive, 10 units fully consumed including all six parts of the 2026-09-07 Takeout export, nothing pending.
- **Dedup coverage bug fixed**: the index had covered only `Library\` and `NoDate\`, 12% of the library, so every one of 21,649 duplicate pairs found so far had compared a staged copy against a settled copy it could not see (`e4812dc`). `INDEX_ROOTS` is now explicit and includes `_Review` on purpose.
- **Disk** (`state/STATE.json`, 2026-09-09T06:45:19): C 172.9 GB free, D 278.8 GB, G 91.0 GB, H 164.3 GB.
- **Governance**: `AGENTS.md` arrived carrying the fleet's shared canon block rendered from `krishanraja/ai-harness`, now at release v2026.09.08.2 (`ed45d5e`, `b29a7c0`, `94eb31b`); this repository's own rules outrank it on structure, naming, voice and stamps.
- **Not in the library yet** (`RESUME.md`, `docs/MIRROR.md`, `docs/rescues/2026-09-06-wd6400.md`): the WD6400 rescue set, corrected to 17,101 files after one zero-byte file was withdrawn (`c3c6a80`), staged on H:, verified at rest and confirmed uploaded, awaiting ingest; a second machine's batch part-ingested; a third H: source set never ingested.
- **Largest open risk** (`RESUME.md` section 3, `docs/MIRROR.md`): there is still no second copy of the library.

## What changed recently

- 2026-09-09 **A doubled root looked like a bigger, correct inventory** (`77d614e`). Why: "INVENTORY_ROOTS had become [ContentLibrary, ContentLibrary\Archive, ContentLibrary\ContentProduction] after the layout migration, and the last two are inside the first, so both were walked twice." The first rebuild reported 70,056 rows for 68,042 real files: "the inventory looked bigger and was wrong, which is the worse of the two failures." Deduping refused to run until every one of 2,014 duplicate pairs was proven identical field-for-field. Also measured: H: reads 10.5 MB/s single-stream and worse, 8.1 MB/s, across four streams, so parallelism cannot speed up the 241 GB still to move.
- 2026-09-09 **9,975 memories restored, 29 left on purpose** (`9d118a1`). Why: a vision pass had found 10,004 keep=yes verdicts in `_Review`. The restore was checked four ways: every journalled destination exists and no source lingers, 25 re-hashed samples still match, nothing landed outside the chronology, and the one number predicted in advance, 29 files with no journalled origin, came back exactly 29, "the check that matters: a different number would mean the model of what happened was wrong." `_Review` 29,092 to 5,844; `Personal` 41,965 to 51,900.
- 2026-09-08 **The dedup index covered 12% of the library, not a stale 100%** (`e4812dc`). Why: correcting learning 27, which had blamed cache staleness, "real and secondary." The index walked only `Library\` and `NoDate\`, 8,036 files, while `Personal\` and `Communal\` held 60,724, 88% of the library, so every one of 21,649 duplicate pairs was a staged copy against a settled copy the index could not see. Rebuilding the cache, the obvious fix, "yields an index that is perfectly current and still blind": same symptom, apparent fix, no error either way. Caught before ingesting 268 GB from H:, "which would have recreated the whole problem at three times the scale."
- 2026-09-08 **The enrichment engine: metadata, then a cheap model, then a human** (`1a6a2ed`). Why: metadata gave dates for 59%, GPS for 39%, a device for 41%, then left 8,262 files undecidable, because a toddler in dinosaur pyjamas and a forwarded flyer with someone's bank details on it are both metadata-stripped JPEGs and "no rule separates them." Keyed on content hash, never path, because paths "have been invalidated by a Personal/Communal split, a rename of the library root, moving every chronology tree into Media\, a D:->H: mirror and 21,649 deletions." Measured 407 input tokens per 512px thumbnail, not the 1,200 estimated, so $0.75 per 1,000 files rather than $1.60.
- 2026-09-08 **Restructure to ContentLibrary, and 18,552 non-memories evicted from the chronology** (`d5470d7`). Why: one root so the mirror to H: has one root too. 81,306 files moved by directory rename, atomic, no bytes copied: "the risk was never the moves, it was the records," 109,207 manifest paths, an origin map and journals all naming paths about to be wrong, all rewritten and verified. `D:\PhotoLibrary` had been hardcoded in 40 of 77 scripts, 85 times; `scripts/paths.py` is now the one place it is defined. Moved to `_Review`, none deleted: 3,022 screenshots, 2,084 web-asset PNGs, 13,446 received WhatsApp files carrying no camera signal at all.
- 2026-09-08 **Vision pass over `_Review` finds 10,004 memories the rules swept out** (`8f05203`). Why: "I was wrong about the WhatsApp bucket. I argued those 13,446 files were 'not photographs you took ... anything precious almost certainly exists elsewhere'. That was correct about provenance and false about content: most are genuine photographs of a life, sent by the people in them." 15,689 files classified for $11.72 in 65 minutes, zero failures. Had the answer been "delete all," about 10,000 memories would have gone; it was not, because Krish asked to see them first.
- 2026-09-08 **Rule 6: the fallback branch that verifies nothing** (`c3c6a80`). Why: a verifier dispatched on file type and sent everything else to a generic branch that "reads the first four bytes and records them as a signature. That branch has no failure mode." A zero-byte `.mpg` passed on that basis and shipped in the WD6400 publish, corrected from 17,102 to 17,101 files; the other 31 empty files in the same run were caught only by accident, because JPEG and MP4 parsing happen to fail on no input. Caught by counting zero-byte files on the finished result afterwards, not by the verifier, which had passed it twice. No data lost: the source had been an empty stub for years.
- 2026-09-08 **The fleet's shared canon reaches this repo** (`ed45d5e`, synced further at `b29a7c0`, `94eb31b`). Why: "Before this, the canon was well governed and had never reached a product repository: this repo referenced it zero times." `AGENTS.md` now carries a marker-delimited block rendered from `krishanraja/ai-harness`, with this repository's own rules stated to outrank it on structure, naming, voice, stamps and archive location. A local edit to the block was reverted the same day in favour of proposing the fix into the canon instead, "an edit that is right for every repository belongs in the canon, not in one copy of it," and a wrong freshness claim in the AGENTS.md header (that NOW.md is reconciled on every push, not just nightly) was corrected once the push-trigger gap was understood (`b160793`).
- 2026-09-07 **Two names for one file is not two copies** (`62d6aa0`, learning 28). Why: "the same wrong answer to 'how much of this drive is duplicated' was produced two different ways" in one evening. A folder listing showed "264 GB across six backup folders that were 94-97% hardlinked into the library, deleting them would have freed 1-3 GB each." Then a hash sample reported "96.8% identical, which is true by construction for two hardlinks to one inode: it was the same file read twice," and was about to justify deleting the library's own entries. "Identity of content is not identity of storage": only distinct `(st_dev, st_ino)` makes duplication reclaimable. Counting by name had inflated 879.5 GB of real data to 1,202.0 GB.
- 2026-09-07 **Guard every deletion, and fix the cache that manufactured 222 GB of duplicates** (`07a81b2`, learning 27). Why: the ingest "checked 35,114 Takeout members against the library index and rejected 382. The rest were filed as new, and roughly 222 GB of them were byte-identical to files already held. Nothing errored." `guarded_delete.py` is now the only sanctioned way to delete outside autopilot: a surviving copy is re-verified at the instant of the unlink. "Reports are never evidence."
- 2026-09-07 **The full 2026-09-07 Takeout export merged and proven, 21 GB reclaimed** (`ee3b550`, `docs/SEGMENTATION.md`). Why: "All six parts ingested and proven: 35,114 media members, 34,732 new, 382 rejected as byte-identical duplicates." Google's Storage-saver re-encodes of media already held at full quality, 6,230 removed (15.01 GB), each re-verified immediately before deletion; the reverse case, 173 files where Google holds the better copy, "is NOT auto-actioned".
- 2026-09-07 **DISM misreports store health** (`08def7e`). Why: "RestoreHealth -> Healthy, CheckHealth -> Healthy, ScanHealth -> Repairable." "Trusting the first two would have closed out a machine still carrying unrepaired component-store corruption."
- 2026-09-07 **Machine-hygiene runbook and profile-root enforcement** (#3, `da5fc76`). Why: scripts writing relative to cwd meant "no command-text check can see it"; a hook "had been dead this way for three months" because a backslash path fails silently in Git Bash.
- 2026-09-07 **The D: to H: mirror planned** (`4a67ff9`, `docs/MIRROR.md`). Why: "The library has no second copy, on a drive that has logged controller errors. This plans fixing that without the fix becoming its own incident."
- 2026-09-07 **Export part numbers are not stable identities** (`60c439d`, learning 26). Why: a fresh Takeout export repartitioned everything; "new 001 carries 3,058 files the library lacks (8.8 GB, 36% of the part) and new 002 carries 4,291 (16.5 GB, 60%). Both would have been skipped as 'already ingested' under the old numbering."
- 2026-09-07 **The WD6400 rescue recorded, and rules 22 to 25 paid for on it** (PR #1: `dff4dc8`, `d99cdf2`, `99781fa`). Why: "Three times, byte size was used as evidence that two files were the same; three times it was wrong." Then the cloud check found "the files robocopy wrote last were absent from the cloud entirely."

## What is next and what is waiting on Krish

- Next (`RESUME.md`; `docs/SEGMENTATION.md`; this run's reading of `tools/track.py`): fix `tools/track.py` so it walks `Media\` before `state/STATE.json` and `state/PROGRESS.md` can report real library figures again (see "Do not trust"), then run `tools/refresh.py`. After that: the second machine's batch, the WD6400 rescue set (now 17,101 files) and the third H: source set, all still un-ingested, then assign a side to anything left in `Pending-Segmentation\` and run the segmentation `docs/SEGMENTATION.md` describes.
- Waiting on Krish (found this run, not yet in any doc): `tools/track.py` counts `Personal\`, `Communal\`, `Library\` and `NoDate\` directly under `D:\ContentLibrary`, but the 2026-09-08 restructure nested them under `Media\` (`scripts/paths.py`). Every run since has reported the library at zero files without erroring, the same invisible-coverage shape as learnings 14, 20, 21 and `e4812dc`.
- Waiting on Krish (`docs/SEGMENTATION.md`, section 3): whether `Archive\` and `ContentProduction\` stay beside the chronology, as the restructure left them, or move inside it; whether "delete all screenshots" overrides the classifier's move-never-delete rule; what the remaining categories are.
- Waiting on Krish (`RESUME.md`, "Waiting on the user", against smaller numbers now): review and empty `_Review\` (5,844 files after the restore, not 10,540); settle the origins with no side assigned; decide whether the mirror may start before the audit is complete.

## Read next

1. `RESUME.md`: where the work is paused, what to do next, and which decisions are closed. Start here, but see "Do not trust" for what it predates.
2. `state/STATE.json` and `state/PROGRESS.md`: the counted position, generated by `tools/track.py`. Library figures are currently unreliable; see "Do not trust".
3. `docs/HANDOVER.md`: what has been consolidated and the data-loss record. Its 2026-09-06 tables predate the restructure below; its timeline paragraph is current to this run.
4. `docs/LEARNINGS.md`: 28 numbered rules, each with its incident. Read before changing any exclusion, deletion or classification rule.
5. `scripts/paths.py`: the current library layout, and the record of the 2026-09-08 migration from the old one.
6. `engine/README.md`: the enrichment engine, new this run: metadata, a cheap vision model, then a human.
7. `docs/ARCHITECTURE.md`: the storage model and the canon protocol for sessions; its layout diagram predates the restructure.
8. `docs/ORGANISING.md`: the schema and retention rubric, written to be applied to every drive in turn.
9. `docs/MIRROR.md`: how D: and H: end up holding the same library, and where the human checks it.
10. `docs/SEGMENTATION.md`: what the library is allowed to contain, and the decisions needed before it starts.
11. `docs/rescues/`: one dated record per source; `2026-09-06-wd6400.md` now carries its post-publication correction.
12. `docs/MACHINE-HYGIENE.md`: bring a Windows machine to the agreed clean state.
13. `AGENTS.md`: entry file for coding agents, carrying the fleet's shared canon block.
14. `docs/history/LOG.md`: what the steward did and when.

## Do not trust

- `state/STATE.json`'s and `state/PROGRESS.md`'s library counts (dated, undated, and the Personal/Communal/Library/NoDate chronology, all zero, generated 2026-09-09T06:45:19): `tools/track.py` walks `Personal\`, `Communal\`, `Library\` and `NoDate\` directly under the `D:\ContentLibrary` root, but the 2026-09-08 restructure (`d5470d7`) nested them one level deeper under `Media\`. The ingest totals and disk-free figures in the same generated files are unaffected and current.
- `docs/ARCHITECTURE.md`, `RESUME.md`, `README.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md`: all describe the pre-restructure `D:\PhotoLibrary` tree (`PhotoLibrary\Personal`, and so on). The code at `d5470d7` moved to `D:\ContentLibrary\Media\...`; `scripts/paths.py` is the current record. The steward has not rewritten these narrative documents; Krish's own reconciliation is the safer path for the parts that need judgement rather than a path substitution.
- The duplicate-count history: `state/STATE.json`'s 10,026 rejected and the Takeout merge's 382 rejected were produced by an index later found to cover only 12% of the library, not merely stale (learning 27 blamed staleness; `e4812dc` found the walk itself missed `Personal\` and `Communal\` entirely). Neither count measures what the library actually holds twice.
- `docs/HANDOVER.md`'s tables ("What has been consolidated", "Library layout as of 2026-09-06"): stamped "Last verified: 2026-09-06", they predate the restructure, the enrichment engine and the review restoration. The new timeline paragraph in that file points past them to `scripts/paths.py` and `state/STATE.json`, but the tables themselves were not re-verified against disk.
- Any free-space, sync-state or throughput figure not measured in the current session: `RESUME.md`, "FIRST, ALWAYS: measure the link". `RESUME.md`'s own overnight disk-free figures (D: 311 GB, C: 67 GB) are older than today's `state/STATE.json` generation (C 172.9, D 278.8, G 91.0, H 164.3) and should not be read together as if from the same moment.
