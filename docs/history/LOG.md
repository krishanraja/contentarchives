# History log

Newest first. Entries are written by the docs steward (see the steward link in
NOW.md) and by humans doing the same job by hand. Nothing in this file
describes current behaviour; NOW.md and the state doc do.

This repo already keeps its chronology in two places the steward does not
duplicate: `docs/rescues/` holds one dated record per source, and
`docs/LEARNINGS.md` holds the numbered rules in the order they were paid for.
This log records what the steward did to the documentation and when.

## 2026-09-16

- reconciled at `75bb274`: `main` gained 54 non-steward commits since the previous reconciliation's stated head (`40f2638`). The largest thread: the stage migration finished, Phase 2 moving every remaining script out of `scripts/` into the stage that owns it (114 code files across twelve stages and `guards/`, none hardcoding a machine path) and Phase 3 moving stages 05-08 plus the chain machinery with `git mv` and a new `stagepath.py` replacing per-file `sys.path` hacks (`ea7668d`, `8814663`, `be64b41`); moving turned up three defects of the same shape, a gating test scanning an emptied directory, a contracts test not knowing `stages/` existed, and one script importing a neighbour before its own bootstrap ran, all fixed before being trusted. Video faces reached completion, 41,931 joined to the frozen clusters and `merge_clusters.py` taught to read them (`05857fb`). Naming continued through rounds 14 to 19: a Communal-faces filter was added and immediately found to have joined on the wrong column, which would have emptied Krish's entire queue instead of Bharti's (`1af33e8`), followed by the clarification that "for Bharti" and "a photograph of Bharti" are different signals (`62dede2`); two long-standing name collisions were resolved by asking rather than inferring, the two Kirans (`9f60b9c`) and the three Rishis (`21c7c47`, `be38131`), both times against what the counts and dates alone would have suggested; and resolving the Rishis surfaced a stale `tags` person layer that no journal answer could override, fixed by dropping only the superseded name from the derived table (`943f979`, "THE RISHI BUG"). Two supervision guards were corrected against measured behaviour: `check_repeats.py`'s baseline was reading zero earlier sheets and reporting every round clean, now reads the append-only journal and refuses on an empty baseline (`75bb274`), and the database rebuild's `-Progress` signal was chosen by replaying a 40-sample recorded trace against four candidates after two earlier picks had already proven wrong (`db12829`, `7075954`). `profile_coverage.py --term` was found silently skipping a name already covered by a shorter, unrelated term (`6115d22`). NOW.md re-headed to `75bb274`, given twelve new "What changed recently" bullets in the commits' own words, its "What it is" section updated for the finished migration and the corrected learning count, two new "Who it is for" story bullets (the stale-tag layer, and the guard that read zero and called it clean), its "Where it is right now" section rewritten end to end for the finished migration, completed video faces, resolved collisions and the two guard fixes, its "What is next" section repointed at round 19 with Krish, and its rule count in "Read next" moved to 55.
- fixed: NOW.md's "What it is" section still described the published `scripts/` as redacted copies carrying 34 pseudonym lines that could not run, and its stage-conformance figures (10 learnings prose-only, five stages missing a test file) were the 2026-09-15 numbers. Both were overtaken by this run's code: `scripts/` now holds nothing, and `tests/test_stage_contracts.py` prints 5 prose-only learnings (8, 13, 17, 24, 29) and 114 of 114 code files owned. Restating a stale figure a test already prints is drift, not maintenance, so both were corrected against the run rather than left.
- checked and dismissed: the digest again flagged the thirteen `STAGE.md` files as a duplicate basename. Unchanged reasoning from 2026-09-15: one `STAGE.md` per stage documenting a different part of the conveyor is the repo's own convention, not a duplicate. Nothing moved.
- waiting on Krish: his answers to round 19, sent this run with a priority queue of 25 clusters behind the 29 photographs the stale-tag fix left unnamed, plus 35 from the ranked pool. The open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user") stand as before.
- left alone: `docs/HANDOVER.md`, `docs/ARCHITECTURE.md`, `README.md`, `docs/MIRROR.md`, `docs/SEGMENTATION.md` and `docs/ROADMAP.md` were all touched by this run's commits, but only for path corrections following the `engine/` and `scripts/` retirement and one added invariant in `docs/ROADMAP.md` ("for Bharti" means Communal) that Krish wrote himself; none of the storage-model or phase-plan content the digest already flags as stamped ahead of the code needed rechecking against this run's changes. `docs/PRODUCT.md` gained a small update from the same path-correction pass; its own content was not otherwise touched.

## 2026-09-15

- reconciled at `40f2638`: `main` gained 9 non-steward commits since the previous reconciliation's stated head (`bdb9f6c`): the repo restructured into a conveyor of twelve stages plus a `guards/` layer, with a test failing the build if a learning has no owner or a cited enforcement stops existing (`40f2638`); a video-faces pipeline walked against all 49 learnings before being armed, finding and fixing 11 real gaps (`6f8463e`, learning 50); the pipeline itself armed to give every video faces rather than one sampled frame, and handed over (`d4fc7af`, `e817c5e`); three rounds of naming landing 169 answers and Bharti's Communal queue defined (`17dca32`); a second round of naming that found `library.db` was silently dropping every name but the last one written on a group photograph, fixed with a `photo_people` table (`cb45ab2`, `db8542e`, learning 49); cluster merging tested against Krish's own named clusters as a free ground truth (`9a27af2`); and the naming page itself found to be showing the wrong face for a cluster in 11 of 12 crops despite a verified underlying dataset (`98ab0ad`, learning 48). NOW.md re-headed to `40f2638`, given six new "What changed recently" bullets in the commits' own words, its "What it is" and "Where it is right now" sections rewritten for the stage-conveyor restructuring and the unattended video-faces run, two "Who it is for" story bullets added for learnings 48 and 50, its "What is next" section repointed at round four and the game once video faces complete, a new "Read next" pointer to `guards/STAGE.md` and `stages/*/STAGE.md`, and its rule count moved to 50.
- "Where it is right now" was substantially shortened rather than left to accumulate: detail on the now-superseded classification, geocoding and Phase 3B work stays in "What changed recently" and in this log, and the section now states only what is true today, per `SCHEMA.md`'s 200-line limit.
- checked and dismissed: the digest flagged thirteen files named `STAGE.md` (`guards/STAGE.md` and one per `stages/*/`) as a duplicate-basename candidate. This is the repo's own convention, one `STAGE.md` per stage documenting a different part of the conveyor, the same pattern as a per-directory `README.md`; not a duplicate or superseded document, and nothing was moved.
- waiting on Krish: nothing tonight while the video-faces chain runs unattended. Next when it completes: round four of naming, then the game (`docs/ROADMAP.md` Phase 8); the open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user") stand as before.
- left alone: `docs/HANDOVER.md`, `docs/ARCHITECTURE.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md` remain stamped or written for the pre-restructure `D:\PhotoLibrary` tree; none of this run's nine commits touch what they describe. `docs/ROADMAP.md` carries this run's own decisions already (`17dca32`, Phase 8) and needed no further edit.

## 2026-09-14

- reconciled at `bdb9f6c`: `main` gained 3 non-steward commits since the previous reconciliation's stated head (`92f5e3c`): Phase 3B completing at 119,830 faces and 79,747 rich descriptions, with a postcondition that read a shard log's startup line and could never pass on a flawless run, fixed to ask the store what is outstanding now instead (`3797746`, learning 47); face embeddings clustered into 44,284 groups on a threshold measured against a free ground-truth control, with the twelve largest checked for coherence before being shown to anyone (`f75bd7c`); and the naming sheet turned into a form that keeps a live tally and hands names back through a script that refuses an unknown cluster id (`bdb9f6c`). NOW.md re-headed to `bdb9f6c`, given three new "What changed recently" bullets in the commits' own words, its "Where it is right now" section rewritten for the completed faces and descriptions passes, the new clustering and the new naming form, one "Who it is for" story bullet extended with the postcondition's mirror-image failure and a new bullet added for the clustering leverage story, its "What is next" section repointed at naming clusters as the open action, and its rule count in "Read next" moved to 47.
- waiting on Krish: naming the largest face clusters through `stages/07_people/people_sheet.py` and `stages/07_people/record_people.py`; the open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user").
- left alone: `RESUME.md`'s "RIGHT NOW" section still describes the state before this run (waiting for `PHASE 3B COMPLETE`, clustering as an unstarted step); it is Krish's own narrative document and the steward does not rewrite it from an outside read of the commit log, per the 2026-09-09 precedent below. `NOW.md`'s "Do not trust" now names the gap. `docs/HANDOVER.md`, `docs/ROADMAP.md` and `state/PROGRESS.md` remain stamped ahead of this run's code changes; none of the three commits touch what those documents describe (the pre-restructure tables, the phase plan, the counted library).

## 2026-09-13

- reconciled at `92f5e3c`: `main` gained 10 non-steward commits since the previous reconciliation's stated head (`587de45`): a chain-gating framework requiring every long step to prove it works, is moving and is postconditioned before it runs unattended (`23fe173`); a dot-sourcing bug removed for changing the calling chain's own semantics (`45c8d2d`); a chain that reported `PHASE 3B COMPLETE` having silently skipped its own supervisor and run no faces (`e411679`, learning 44); a measurement showing parallelism was making face detection slower, not faster (`11629a4`); a watcher that deadlocked by logging into the file it was watching (`f52b181`); a face-embedding file corrupted by two buffers drifting out of position, 264 of 11,611 images trustworthy (`af0829a`, learning 45); a mandatory `-Verify` step added after a checkpoint built to confirm correctness instead confirmed only motion for five hours over the same corruption (`d5d3a6a`, learning 46); RESUME.md rewritten for a session with the Anthropic API turned off, verifying nothing left to run needs it (`f7d1658`); the face coverage gap measured at about 157 images and left unchased (`ce9f54e`); and a $46 description pass added beside faces, after a prompt-routing bug that would have shipped old four-word labels under the new field names was caught before it ran (`92f5e3c`). NOW.md re-headed to `92f5e3c`, given ten new "What changed recently" bullets in the commits' own words, its "Where it is right now" section rewritten around the chain-gating escalation, the restarted single-shard face run, the new description pass and the Anthropic API being turned off, one "Who it is for" story bullet extended with the day's three further instances of the same lesson, its "What is next" section updated for the two tasks now running unattended, and its rule count in "Read next" moved to 46.
- waiting on Krish: the open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user"). Nothing is blocked on him tonight while faces and the description pass run unattended.
- left alone: `docs/HANDOVER.md`, `docs/ROADMAP.md` and `state/PROGRESS.md` are stamped ahead of this run's code changes, but none of the ten commits touch what those documents describe (the migration, the phase plan, the counted library). Re-stamping without re-checking the body against disk would be drift, not maintenance; `NOW.md`'s "Do not trust" already carries the gap for `state/PROGRESS.md` and `docs/HANDOVER.md`.

## 2026-09-12

- reconciled at `587de45`: `main` gained 11 non-steward commits since the previous reconciliation's stated head (`b7db045`), though NOW.md's prose had already absorbed the first of them (`8031119`) without its head field being bumped, which is what the strict validator caught. The other ten: two safeguards for doing the wrong thing confidently rather than crashing or overspending, written after two of that day's own bugs were found the same way (`587de45`); a logging helper that made every skipped step run anyway (`5f360b0`); a retry loop that would have re-judged 19,692 files sixty times (`0e1c89e`); geocoding applied and `library.db` built, with a vocabulary bug caught before it shipped a false claim (`6bef342`); a test proving the answers-journal invariant survives deleting the database entirely (`7217055`); phase 9 (asking) written into `docs/ROADMAP.md` and `RESUME.md` rewritten for the evening's real position (`98aae8d`); `stages/05_enrich/geocode.py`, `stages/07_people/answers.py` and `docs/PRODUCT.md` added (`c5a5ff4`); ffprobe found resolving under the wrong Windows username, costing 12,988 videos their duration (`4b05020`); an orphaned classifier still billing after a re-arm (`6d30bc9`); and a scheduled task killed anyway, answered with a halt file rather than a bigger hammer (`0fafdf3`). NOW.md re-headed to `587de45`, given ten new "What changed recently" bullets in the commits' own words, its "Where it is right now" section rewritten around the completed classification pass and the running face detection, one "Who it is for" story bullet extended with the day's three instances of the same lesson, and its "What is next" section updated now the drive-letter swap it named is done. `docs/PRODUCT.md`, new this run, was added to "Read next" and to the `README.md` doc index.
- fixed: NOW.md had three em dashes, one at the top of "What changed recently" and two further down in existing bullets; the validator's report had named only the first it found. All three replaced with commas or plain punctuation.
- fixed: `RESUME.md` said to run `geocode_library.py` and `build_db.py` "when PHASE 3B COMPLETE appears", but both had already been run once, ad hoc, earlier the same day while phase 3 was still running (`c5a5ff4`, `6bef342`). Added a note naming both as idempotent, so the instruction still holds for the next session and reads as a safe re-run rather than an undone step.
- waiting on Krish: the open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user"). Nothing is blocked on him tonight while the face pass runs unattended.

## 2026-09-11

- reconciled at `8a6b31b`: `main` gained 10 non-steward commits since the previous reconciliation merged (`edc555f`): the migration's copy was verified, with one file recopied after landing at the right size and the wrong bytes (`fb7292a`); the verify was chained straight onto the copy (`0599c9e`); the remaining migration steps were turned into commands for the next session (`77ae757`); the published-scripts gap was measured at 66 of 103 missing, including the segmentation pair, and 21 were added (`01e6e57`, `8a66647`); Krish gave standing approval to push to main by default (`a502222`); Gemini 3.1 Flash-Lite went live as `stages/05_enrich/classify_live.py` (`cdde607`); `stages/08_index/master_sheet.py` built `MASTER.csv` for the first time, moving measured coverage from 49.8% to 59.2% (`cca3501`); the eight-phase roadmap was written (`028eda7`, `docs/ROADMAP.md`); and roadmap phases 1 to 3 were chained to run unattended (`8a6b31b`). NOW.md re-headed to `8a6b31b`, given nine new "What changed recently" bullets in the commits' own words, its "Where it is right now" section rewritten for the verified copy and the running phases, its "What is next" section restructured around the roadmap's phase order, and one existing "Who it is for" story bullet extended with this run's instance of the same lesson. `README.md`'s doc index gained a row for `docs/ROADMAP.md`, new this run and not yet linked from it.
- fixed: NOW.md had 24 em dashes. Most were introduced by Krish's own direct edits to the file in commits since the last reconciliation (`a8f36ef`, `cc4e92f`), which touched "Where it is right now" while fixing the migration tooling; the validator rejects any occurrence and had not run strict since. All replaced with hyphens.
- waiting on Krish: swap the drive letters so the LaCie becomes `D:` (`RESUME.md` step 3, needs an elevated shell); the open decisions named in `docs/ROADMAP.md` ("Open decisions") and `RESUME.md` ("Waiting on the user").

## 2026-09-09

- reconciled at `77d614e`: `main` gained 15 non-steward commits since `62d6aa0` (2026-09-08 to 2026-09-09): the library restructured to a single `D:\ContentLibrary` root with `Media\` holding the chronology and 18,552 non-memories evicted to `_Review` (`d5470d7`); a dedup-index coverage bug found and fixed, the index having covered only 12% of the library (`e4812dc`); the enrichment engine added under `engine/` (`1a6a2ed`); a vision pass over `_Review` finding 10,004 misclassified memories (`8f05203`) and 9,975 of them restored (`9d118a1`); the on-disk inventory rebuilt to 68,042 files after a doubled root inflated it (`77d614e`); `AGENTS.md` added carrying the fleet's shared canon block and synced twice (`ed45d5e`, `b29a7c0`, `94eb31b`); and rule 6 in `docs/LEARNINGS.md` given a fallback-branch addendum (`c3c6a80`). `NOW.md` re-headed to `77d614e`, given nine new "What changed recently" bullets in the commits' own words, two new "Who it is for" story bullets, and its "Do not trust" section rewritten around the restructure. `docs/HANDOVER.md` gained a second dated timeline paragraph covering these commits and a pointer away from its now-superseded layout tables to `guards/paths.py`; its "Last verified" stamp stays at 2026-09-06 since the tables themselves were not re-verified against disk. `README.md`'s doc index gained rows for `AGENTS.md` and `engine/README.md`, both new since the last run.
- waiting on Krish: `docs/ARCHITECTURE.md`, `RESUME.md`, `README.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md` all still describe the pre-restructure `D:\PhotoLibrary` tree; the code at `d5470d7` moved to `D:\ContentLibrary\Media\...` (`guards/paths.py`). The steward left these narrative documents as they are rather than rewrite Krish's own account of the project from an outside read of the commit log; `NOW.md`'s "Do not trust" names the gap.
- waiting on Krish: `state/STATE.json` and `state/PROGRESS.md` report the library as zero files. `tools/track.py` walks `Personal\`, `Communal\`, `Library\` and `NoDate\` directly under the new `D:\ContentLibrary` root, but the restructure nested them one level deeper under `Media\`; the counter is not looking where the files now live. No document was changed to compensate; `NOW.md` quotes the whole-library count from a commit message instead.

## 2026-09-07

- reconciled at `62d6aa0`: `main` gained Krish's commit "Learning 28: two names for one file is not two copies", adding rule 28 to `docs/LEARNINGS.md` and rewriting RESUME.md's "Where things stand" section from midday to overnight, folding in the Takeout merge and the guarded-delete fix that had already landed. NOW.md re-headed, given a "What changed recently" bullet in the commit's own words, a story bullet added under "Who it is for", and the rule count in "Read next" moved to 28. The "midday" references in "Where it is right now" and "What is next" were updated to overnight, since RESUME.md no longer predates the Takeout merge. The now-resolved "Do not trust" line about that stale section was replaced with one naming the unreconciled gap between state/STATE.json's disk-free figures (generated at `ee3b550`) and RESUME.md's later, unregenerated overnight figures.
- reconciled at `07a81b2`: `main` gained Krish's commit "Guard every deletion, and fix the cache that manufactured 222 GB of duplicates" (learning 27, `guarded_delete.py`, `reclaim_duplicates.py`). NOW.md re-headed, given the bullet in the commit's own words, its duplicate counts marked as undercounts, the reclaim added to "What is next", and the rule count in "Read next" moved to 27. The steward-secret reminder was removed from "What is waiting on Krish" at Krish's instruction, the secret being in place.
- reconciled at `ee3b550`: `main` moved by one non-steward commit ("Merge the full 2026-09-07 Takeout export, verified, and reclaim 21 GB") while the bootstrap was under review. `NOW.md` was re-pointed at `ee3b550` before the first push, its figures re-quoted from the regenerated `state/STATE.json` (2026-09-07T19:14:52), a bullet added for the merge, and `docs/SEGMENTATION.md` (new in that commit) added to "Read next" and the README index. The HANDOVER timeline paragraph gained one sentence for the same merge.
- decision: docs steward adopted for this repo (Krish, 2026-09-07). Bootstrapped by hand at `08def7e`; `.github/workflows/docs-steward.yml` calls the shared workflow in `krishanraja/control-center` on every push to `main` and nightly at 20:35 UTC.
- indexed: `docs/rescues/` is the per-source chronology, one dated file per source (`2026-09-06-wd6400.md` is the first and the model). The steward does not move or duplicate these files.
- indexed: `docs/LEARNINGS.md` is the append-only rule chronology, 26 numbered rules at `08def7e`, each with the incident that produced it. The steward never edits an existing rule.
- reconciled at `08def7e`: `NOW.md` written for the first time per `docs/steward/SCHEMA.md`. Figures quoted only from `state/STATE.json` and `state/PROGRESS.md`, never narrated (re-quoted at `ee3b550`, see above).
- reconciled at `08def7e`: `docs/HANDOVER.md` gained one dated timeline paragraph near the top covering the repo's commits of 2026-09-06 and 2026-09-07. Its "Last verified" stamp was left at 2026-09-06 because the rest of the body was not re-verified against disk; its tables predate the state file at HEAD, and NOW.md's "Do not trust" says so.
- reconciled at `08def7e`: `README.md` doc index gained rows for `NOW.md`, `docs/history/LOG.md`, `docs/MIRROR.md` and `docs/SEGMENTATION.md` (the last two had no index row).
- waiting on Krish: `docs/HANDOVER.md` has drifted from the state file at HEAD. Its tables (59,450 files, "Library 328", verified 2026-09-06) predate `state/STATE.json` (90,408 files, generated 2026-09-07T19:14:52), and its outstanding item 3 describes the 2026-09-05 Takeout export as still downloading where `RESUME.md` section 2 says that export is dead and `ee3b550` says the replacement is fully ingested. The steward cannot recount from disk, so the figures were left as they are and NOW.md's "Do not trust" names the drift. A session on the library machine should run `tools/refresh.py` and revise the handover.
- reconciled at `08def7e`: no duplicate, unlabelled or stale-stamped document found. The steward digest for this range reported "Duplicate and unlabelled candidates: None." No file was moved.
