__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# never commit real profiles - they contain personal terms and machine paths
profiles/*.yaml
!profiles/example.yaml

# working artefacts
*.log
*.csv
!docs/*.csv
!state/*.csv
_scratch/

# the redaction key reverses the pseudonyms in state/ - it must never be committed
redaction-key.json
*redaction-key*

# per-file maps and document shortlists name real people and real paperwork.
# They stay on the machine that holds the library.
ORIGIN-MAP.csv
origin-map.csv*
SENSITIVE-FILES.csv
